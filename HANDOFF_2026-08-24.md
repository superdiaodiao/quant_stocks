# Quant Stocks research handoff — 2026-08-24

## 0. Read this first

This is the continuation contract for the next Codex task. It records the
user's full goal, strict research rules, current reproducible state, unresolved
work, and conclusions that must not be overstated.

Current decision (superseded on 2026-08-30 by the v48 checkpoint below):

- `policy_status=V22_FROZEN_RESEARCH_FORWARD_READY`
- `release_status=BLOCKED`
- `promotion_eligible=false`
- not ready for live trading
- the v22 future-only research observation is frozen but not started; only
  market data after `2026-08-30` may count
- no broker connection, order creation, GitHub trading workflow, or automatic
  trading has been authorized
- do not tune parameters while historical inputs are changing
- do not expand the fixed 18-configuration diagnostic grid
- retain correct source repairs even when they make the backtest worse

Bulky mutable research data stays local. The explicitly enumerated immutable
protocols, compact audit results, implementation, tests, and this handoff may
enter the Git checkpoint; staging bundles and unrelated generated data may not.

### 2026-08-30 current checkpoint: v48 replaces v22/v43

The operative research model is now
`v48-isolated-v47-entry20-portfolio25`, not v22 or v43. The version number is
an immutable experiment/audit sequence, not a count of strategy upgrades:
v23-v26 developed the stock selector, v27 was a 2026 diagnostic, v28 added the
individual stop, v29-v30 repaired/adjudicated the 2019 identity path, v31-v41
were rejected risk overlays, v42-v43 built and repaired the prospective
runtime, v44-v47 tested the final predeclared refinements, and v48 froze the
successful v47 model for future observation.

Current binding:

- v47 model: monthly equal-weight Top 5 individual stocks from the frozen v26
  selector, a 20% loss stop from each stock's latest monthly entry close, a
  25% monthly-reset portfolio trailing stop, and cash after a stop until the
  next frozen monthly target
- v47 development result: `PASS`; at 50bps it beat Nasdaq in every 2020-2025
  training year and its maximum drawdown was 32.8123%, within the frozen
  33.6408% ceiling
- v48 protocol SHA256:
  `e717ff441b9b3804579df35fc470d5f961d8b09402691a3704de1dca649978ef`
- v48 ledger contains only `PROTOCOL_FROZEN`; first allowed prospective signal
  date is `2026-08-31`
- v43 was superseded with zero signals, zero executions, and zero valuations;
  no prospective result was discarded
- 2019 supports the base selector and adjudicated identity path; 2020-2025 are
  training/model-selection data; the reused 2026 January-July diagnostic is
  exposed. All of them count as zero official forward wins.
- `release_status=BLOCKED`, `promotion_eligible=false`; no broker connection,
  order, or capital allocation is authorized

The forward runner scores complete prospective months, so it does not require
waiting for a full calendar year. Use the first complete month as an
operational sanity check and three complete months as the first decision
checkpoint. v49 subsequently tested whether the already available 2026
January-July period could shorten that checkpoint to one month. It failed the
precommitted relative-drawdown gate, so three complete true-prospective months
remain required. Do not change the frozen model in response to either result.

## 1. User's full objective

The objective is not merely to preserve v4 Candidate 15 or wait one year:

1. Make the 2019-2021 historical path as complete and usable as later years,
   while preserving explicit evidence for gaps that cannot be recovered without
   hindsight or incompatible accounting bases.
2. Keep the historical security universe point-in-time (PIT) correct, including
   ordinary-share filtering, warrants/units/rights exclusion, dated exchange
   membership, ticker/name changes, delistings, and terminal values. A current
   Nasdaq list must never be projected backward as historical truth.
3. Once data is stable, train strategy selection afresh rather than assuming
   today's fixed parameters are optimal. The procedure should adapt inside a
   pre-frozen search policy, use walk-forward or nested time splits, and defend
   against selection overfit.
4. Separate training, validation, and a final untouched historical holdout.
   Repaired 2019-2021 data should become genuine evidence, not be discarded.
5. If historical completeness and robustness gates pass, replace the former
   one-year forward wait with the shortest defensible genuine forward phase.
   The working target is roughly 3-6 months, with pre-frozen thresholds and no
   parameter changes. Elapsed time alone is not evidence.
6. If all gates pass, move toward live use through a small-capital staged
   rollout with exposure limits and kill switches. Brokerage access and actual
   orders require later explicit authorization.

Immediate milestone: preserve the frozen v48 configuration, record only new
signals starting on `2026-08-31`, complete one full prospective month as the
operational sanity check, and use three complete months as the first decision
checkpoint. Before any execution-cost claim, replace the account-size
sensitivity with the user's actual equity and IBKR pricing plan. Do not shorten
the phase by reclassifying already inspected history as forward evidence.

## 2. Repository and immutable-data checkpoint

At the beginning of this continuation:

- branch: `master`
- local HEAD and `origin/master`:
  `e3cfa8f8948962e04d08f62acfeaff7327632c0f`
- preceding implementation checkpoint:
  `2c4fb1e3fff48386ad7502fff55fc0362ff2ed19`
- preceding handoff checkpoint:
  `e3cfa8f8948962e04d08f62acfeaff7327632c0f`

At the beginning of the 2026-08-27 continuation, local `master`,
`origin/master`, and remote `refs/heads/master` were all
`d7348fb78ab53c6690c4cdbea8506ff81ec69498`.

Research candidates never replace the formal financial files. Invariants:

- annual: `62f6c624b2fac85118ea6d49646870f1a56fa053687cb617dc468d856e19c34d`
- quarterly: `1be16a6342217d6771eca7d2ca49156e726b424ebfbfe7b90dfa6c232ea8bf69`
- annual coverage: `275664a817d430767de47af430c7a6265670c1d541c6170ee4d120ff9f3e0e21`
- quarterly coverage: `40c33be0f82c4ee4a55ba7f9650773620e4f8d284c17e9c419336013ce640bf2`

Earlier foundation still relevant:

- 1,465 scoped tickers have an explicit SEC Company Facts state: 1,449 raw
  payloads, 11 official-unavailable evidence records, and 5 absent from the
  exact SEC ticker map.
- the manifest binds 1,756 CIK payloads, including historical CIKs, plus a
  refresh-state SHA for checkpointing and offline replay.
- stricter research-only proven data previously contained 253,810 annual and
  314,615 quarterly rows without replacing formal files.
- old v4 Candidate 15 traversed 194 selected symbols and 508 holdings with zero
  missing holding-trading days and zero unresolved delisting terminal values.
  This proves its traversed path, not the whole market or expanded price overlay.
- Wayback Company Facts variants were audited, archived in a GitHub Release,
  restored in isolation, and about 32GB of redundant local copies removed.

## 3. Latest reproducible candidate

Directory:

`output/research_only/v14/candidate_fundamentals_v14_checkpoint_20260829_sohu_restated_quarters/`

Bindings:

- candidate annual SHA256:
  `f49fe5777baf2a64331a0cbe54bee794e8c2819d5dd732fb7dca017b2d2a95ce`
- candidate quarterly rows: `406,413`
- candidate quarterly SHA256:
  `a9df5975908c7416b7e0902a8adeef02cc32b25c83367999687d9992b45d5caf`
- candidate manifest SHA256:
  `f2489fa0ed95b4f0f9a39d8c54e7b9244290bec199f2767af0c634b5b7eb50a0`
- price overlay: `output/research_only/v14/price_overlay/`
- universe snapshots: `output/research_only/v14/universe_snapshots/`
- final audit:
  `output/research_only/v14/checkpoint_20260829_sohu_restated_quarters_recovered.json`
- final audit SHA256:
  `70ae7458cdcf785d69942acc46782b99bca3fd70c4b67b0348c5f68e178295a1`
- final financial-priority CSV SHA256:
  `da109905c70d36898fe8f2689275fc272e69d69a0451f9866de59e6eb8beedca`
- final price-priority CSV SHA256:
  `79aca5f47fad5d5fc9dd9057787cbea3bf05640cc473b81b0aae2102bd0d7959`

Always audit this path with the research price overlay. That overlay recovered
all observable price competitors and exposed financial gaps formerly hidden by
absent prices; a larger older gap count was improved observability, not damage.

## 4. Work completed in this continuation

Twenty-eight source-locked recovery packages were added, the pre-existing GLPG
package was extended with a complete Q3 growth bundle, the pre-existing ADUS
negative package was upgraded to deterministic current-audit evidence, and
the pre-existing GGAL IAS-29 package was extended with complete 2019 negative
evidence. The pre-existing ARGX quarterly package now also carries complete
currency-boundary negative evidence. The pre-existing BCTX package was
extended with exact signal-date TTM evidence and a zero-denominator guard.
The pre-existing DKNG historical-CIK package was extended with exact
pre-signal direct TTM facts that eliminate its remaining missing observation.
The new FORR package uses the issuer-disclosed previous-guidance transition
table to reconstruct a homogeneous ASC 605 TTM bundle and eliminate its stale
growth observation without cross-basis arithmetic.
The new GCMG package uses pre-signal selected historical actuals and explicit
recapitalization continuity to prove a predecessor-parent TTM loss without
using the separate pro-forma or adjusted tables.
The new HIMX package uses its audited 2020 20-F and exact 2020/2021 Q3
earnings-exhibit nine-month comparators to recover a complete IFRS TTM growth
bundle. It preserves the issuer's warning that the interim packages omit a
complete set of IFRS interim disclosures and excludes the separate non-IFRS
reconciliation tables.
The new HRMY package uses its audited 2020 10-K plus exact 2020/2021 Q3 10-Q
nine-month comparators to recover a complete US-GAAP TTM growth bundle. It
source-locks the explicit zero-revenue markers and keeps consolidated net
income separate from preferred/common-stockholder loss.
The new HRZN package corrects the generic SEC parser's BDC classification:
its audited 2020 10-K directly reports comparable 2020/2019 total investment
income and consolidated net increase in net assets from operations, which form
a complete annual TTM growth bundle without later data.
The new LAKE package recovers the pre-signal `Net sales` row omitted by the
generic parser and combines the audited annual comparison with exact Q3
nine-month comparators on one consolidated US-GAAP basis.
The new LGO package recovers audited FY2020/FY2019 IFRS statements incorporated
into the issuer's pre-listing Form 40-F registration and respects the explicit
USD presentation-currency restatement of the comparative year.
HCM's and SMPL's remaining gaps were exhaustively classified as unrecoverable.
Financial packages reject later
filings, adjusted/non-GAAP metrics, incompatible currencies, and incomparable
accounting bases.

### NIU exact TTM growth

- `scripts/research_v14_niu_exact_ttm_growth.py`
- `tests/test_research_v14_niu_exact_ttm_growth.py`
- recovered all 10 observations; CNY US-GAAP direct exact TTM
- 2020-03-31: revenue `+17.698814%`, net income `+155.140785%`
- 2020-06-30: revenue `+14.504399%`, net income `+447.012165%`
- rejects all 14 USD convenience-translation values

### DOYU exact TTM growth

- `scripts/research_v14_doyu_exact_ttm_growth.py`
- `tests/test_research_v14_doyu_exact_ttm_growth.py`
- recovered all 18 observations at 2020-07-31, 2020-08-31, 2020-10-30
- CNY US-GAAP consolidated basis
- first signal: revenue TTM `8,072.142m` (`+80.3015%`), net income
  `269.723m` (`+138.3967%`)
- later signals: revenue `8,707.565m` (`+56.9823%`), net income
  `565.837m` (`+225.5725%`)

### BZUN exact TTM growth

- `scripts/research_v14_bzun_exact_ttm_growth.py`
- `tests/test_research_v14_bzun_exact_ttm_growth.py`
- recovered all 18 observations; RMB thousands, US-GAAP, attributable NI
- 2019-03-31: revenue `+35.0156%`, NI `+35.4601%`
- 2020-06-30: revenue `+26.3219%`, NI `-5.2852%`
- 2020-09-30: revenue `+23.7900%`, NI `-0.3490%`

### CLLS direct TTM loss

- `scripts/research_v14_clls_direct_ttm_loss.py`
- `tests/test_research_v14_clls_direct_ttm_loss.py`
- recovered 4 observations as known nonpositive
- exact IFRS consolidated TTM:
  `-115,212k - (-54,461k) + (-19,290k) = -80,041k USD`
- available 2020-08-05

### BCTX exact pre-signal TTM evidence, intentionally partial

- `scripts/research_v14_bctx_annual_ttm_loss.py`
- `tests/test_research_v14_bctx_annual_ttm_loss.py`
- SHA-locked four SEC sources: FY2020/FY2019 audited CAD losses, the exact
  2020/2019 and 2021/2020 nine-month comparators, and the later FY2021 USD loss
- recovered three exact facts available 2021-06-30 for the 2021-08-31 signal:
  net-income TTM `2,120,642 CAD`, net-income growth `+135.680999%`, and revenue
  TTM `0 CAD`
- current TTM closes as `-4,944,221 - (-4,248,670) + 2,816,193 = 2,120,642`;
  prior TTM closes as `-5,789,662 - (-4,094,995) + (-4,248,670) = -5,943,337`
- revenue growth remains intentionally absent because both current and prior
  revenue TTM are zero; `0/0` is undefined and must never be emitted as `0%`
- retained the audited FY2021 IFRS loss `-428,334 USD`, filed 2021-11-16, only
  for the 2021-11-30 signal; it is not projected backward
- the candidate gained three rows, but all six audit scenarios and the
  financial-priority CSV remain unchanged because the frozen growth selector
  requires all four direct fields; BCTX's three repeated observations are now
  source-exhausted rather than fillable

### DKNG pre-signal direct TTM recovery

- `scripts/research_v14_dkng_historical_cik_quarters.py`
- `tests/test_research_v14_dkng_historical_cik_quarters.py`
- retained the existing 16 exact historical-CIK quarter facts for 2019-2020
- SHA-locked the historical Company Facts payload and the 2020-03-12 S-4/A;
  the S-4/A table is explicitly audited Old DraftKings historical actuals
  presented before and without pro-forma merger effects
- combined audited FY2019 actuals with the timely 2020Q2 H1 comparisons:
  revenue TTM `323.410m - 125.482m + 159.473m = 357.401m USD`; net-income TTM
  `-142.734m - (-57.667m) + (-230.117m) = -315.184m USD`
- both direct TTM facts are available 2020-08-14, 47 days before the
  2020-09-30 signal; the exact net loss deterministically fails the positive
  profit gate, so missing 2018 growth history is no longer decision-relevant
- recovered all three repeated $2m observations; DKNG is absent from the final
  financial-priority CSV and the missing-symbol union fell from 26 to 25
- no S-4/A pro-forma table, Diamond Eagle shell fact, later 10-K/A, or current
  CIK value was used

### FORR comparable ASC 605 TTM growth recovery

- `scripts/research_v14_forr_asc605_ttm_growth.py`
- `tests/test_research_v14_forr_asc605_ttm_growth.py`
- SHA-locked the original 2017Q3 10-Q, 2017 10-K, and 2018Q3 10-Q; the
  original nine-month comparator, audited annual totals, and selected
  quarterly data close exactly
- FORR adopted ASC 606 on 2018-01-01 using modified retrospective treatment,
  so as-reported 2018 values were not spliced into ASC 605 history
- used the issuer's exact 2018Q3 transition-table amounts as if previous ASC
  605 guidance remained in effect: M9 revenue `260.480m USD` and net income
  `10.980m USD`; as-reported ASC 606 values `258.992m` and `10.005m` were
  verified but excluded
- current comparable revenue TTM is `90.377m + 260.480m = 350.857m USD`,
  versus prior `83.446m + 247.296m = 330.742m`, growth `+6.081780%`
- current comparable net-income TTM is `2.093m + 10.980m = 13.073m USD`,
  versus prior `5.790m + 13.047m = 18.837m`, growth `-30.599352%`
- the bundle was available 2018-11-06, 114 days before the 2019-02-28 signal;
  all three repeated $2m observations resolve, FORR is absent from the final
  priorities, and the missing-symbol union fell from 25 to 24
- the candidate overlay added only four direct metrics; annual data, price
  priorities, price-overlay binding, and formal financial files are unchanged

### GCMG predecessor-attributable direct TTM loss

- `scripts/research_v14_gcmg_predecessor_ttm_loss.py`
- `tests/test_research_v14_gcmg_predecessor_ttm_loss.py`
- SHA-locked the 2020-11-19 8-K earnings exhibit and the 2020-12-04 S-1;
  their M9 2020 revenue, consolidated income, and attributable-income actuals
  cross-check exactly
- the S-1 explicitly says the transaction is a GAAP recapitalization, the
  combined financial statements continue the GCM Companies' history at
  historical cost, and no goodwill or other intangible assets are recorded
- used only the S-1's Selected Historical Financial Information actuals:
  attributable TTM `46.777m - 41.430m + (-7.702m) = -2.355m USD`
- consolidated net-income TTM is positive `8.047m USD`, but it includes
  earnings allocated away to redeemable and other noncontrolling interests;
  the selected predecessor-parent line is the consistent common-owner metric
- the direct loss was available 2020-12-04, 27 days before the signal, and
  deterministically fails the positive-profit gate without a growth rate
- all three repeated $2m observations resolve, GCMG is absent from the final
  priorities, and the missing-symbol union fell from 24 to 23
- the separate unaudited pro-forma tables, adjusted FRE/net income, later 10-K,
  and transaction estimates were excluded; the candidate added only one row

### HIMX exact IFRS annual-plus-nine-month TTM growth recovery

- `scripts/research_v14_himx_ifrs_ttm_growth.py`
- `tests/test_research_v14_himx_ifrs_ttm_growth.py`
- SHA-locked the audited FY2020 20-F plus the Q3 2020 and Q3 2021 6-K
  earnings exhibits; all annual and nine-month revenue/consolidated-profit rows
  are parsed and checked from the immutable SEC documents
- aligned net income with the candidate's `ifrs-full ProfitLoss` metric:
  consolidated profit for the year/period, not the distinct
  stockholder-attributable line
- current TTM revenue is
  `887.282m - 611.512m + 1,095.202m = 1,370.972m USD`; prior TTM is
  `671.835m - 496.906m + 611.512m = 786.441m`, growth `+74.326110%`
- current TTM consolidated profit is
  `45.160m - 11.814m + 292.463m = 325.809m USD`; prior TTM is
  `-16.184m - (-16.749m) + 11.814m = 12.379m`, growth `+2,531.949269%`
- the unusually large profit growth is retained because the exact prior TTM
  denominator is positive `12.379m`; no zero denominator, estimate, or adjusted
  value is used
- the bundle was public 2021-11-04, 57 days before the 2021-12-31 signal; both
  age150 observations resolve, HIMX is absent from the final priorities, and
  the missing-symbol union fell from 23 to 22
- the interim exhibits state that they omit the disclosures required for full
  IFRS interim compliance; the package records that limitation and uses only
  the exact issuer-reported statement values. Non-IFRS reconciliation rows,
  adjusted amounts, stockholder-attributable profit, and later FY2021 results
  are excluded

### HRMY exact US-GAAP annual-plus-nine-month TTM growth recovery

- `scripts/research_v14_hrmy_annual_m9_ttm_growth.py`
- `tests/test_research_v14_hrmy_annual_m9_ttm_growth.py`
- SHA-locked the audited FY2020 10-K and Q3 2020/Q3 2021 10-Q filings; parsed
  six exact annual/quarterly statement rows and independently verified the two
  em-dash zero markers on the M9 2019 revenue row
- aligned net income to consolidated `NetIncomeLoss`, not net loss available to
  common stockholders or preferred-stock dividend accumulation
- current TTM revenue is
  `159.742m - 103.454m + 214.227m = 270.515m USD`; prior TTM is
  `5.995m - 0 + 103.454m = 109.449m`, growth `+147.160778%`
- current TTM net income is
  `-36.944m - (-36.736m) + 11.883m = 11.675m USD`; prior TTM is
  `-151.977m - (-115.537m) + (-36.736m) = -73.176m`, growth
  `+115.954685%` under the frozen `(current-prior)/abs(prior)` rule
- all operands were public by 2021-11-09, 52 days before the signal; both
  age150 observations resolve, HRMY is absent from final priorities, and the
  missing-symbol union fell from 22 to 21
- no adjusted value, earnings-release alternative, common-stockholder loss,
  later FY2021 result, estimate, or inferred pre-launch revenue is used

### HRZN exact audited BDC annual TTM growth recovery

- `scripts/research_v14_hrzn_exact_annual_growth.py`
- `tests/test_research_v14_hrzn_exact_annual_growth.py`
- SHA-locked the 2020 10-K filed 2021-03-02, accession
  `0001558370-21-002265`, and verified its audited 2020/2019/2018 statement
  rows for both selected metrics
- mapped BDC `Total investment income` to the operating top line and
  consolidated `Net increase in net assets resulting from operations` to the
  GAAP bottom line; `Net investment income` and adjusted alternatives remain
  excluded
- FY2020 revenue TTM is `46.035m USD` versus `43.125m` in FY2019, growth
  `+6.747826%`; FY2020 net income TTM is `6.364m USD` versus `19.498m`, growth
  `-67.360755%` under the frozen absolute-denominator rule
- the filing was public 59 days before the 2021-04-30 signal; all three
  age150/365/550 observations resolve, HRZN is absent from final priorities,
  and the missing-symbol union fell from 21 to 20
- evidence manifest SHA256:
  `f7016a53e6c81863d267e027e85379072c6da1f1c17599cc02260bee7f22d443`

### LAKE exact US-GAAP annual-plus-nine-month TTM growth recovery

- `scripts/research_v14_lake_annual_m9_ttm_growth.py`
- `tests/test_research_v14_lake_annual_m9_ttm_growth.py`
- SHA-locked the FY2019 10-K and Q3 2018/Q3 2019 10-Q filings; verified six
  exact statement rows for annual and nine-month `Net sales` and consolidated
  `Net income`
- current TTM revenue is `99.011m - 73.970m + 79.620m = 104.661m USD`;
  prior TTM is `95.987m - 70.831m + 73.970m = 99.126m`, growth
  `+5.583802%`
- current TTM net income is `1.459m - 3.386m + 2.076m = 0.149m USD`;
  prior TTM is `0.440m - 5.358m + 3.386m = -1.532m`, growth
  `+109.725849%` under the frozen absolute-denominator rule
- all operands were public by 2019-12-09, 81 days before the 2020-02-28
  signal; all three age150/365/550 observations resolve, LAKE is absent from
  final priorities, and the missing-symbol union fell from 20 to 19
- later FY2020 results, adjusted values, discontinued-operation alternatives,
  and estimates are excluded; evidence manifest SHA256:
  `10ded6b689fb28887b7173519f99904985505d61a5e6d71669871ec45c7c8f9c`

### LGO exact audited IFRS direct annual TTM growth recovery

- `scripts/research_v14_lgo_exact_annual_growth.py`
- `tests/test_research_v14_lgo_exact_annual_growth.py`
- SHA-locked the 2021-04-14 Form 40-F registration cover and its incorporated
  Exhibit 99.69 audited annual statements, accession
  `0001104659-21-049967`; verified exact `Revenues` and consolidated
  `Net income (loss)` rows
- FY2020 revenue is `119.987m USD` versus `105.107m` in FY2019, growth
  `+14.157002%`; FY2020 net income is `6.761m USD` versus a `-26.970m`
  loss, growth `+125.068595%` under the frozen absolute-denominator rule
- the source explicitly says presentation changed to USD and prior-period
  comparative information was restated in USD; it separately records the
  prospective functional-currency change, so no estimated FX splice is used
- the filing was public 77 days before the 2021-06-30 signal; all three
  age150/365/550 observations resolve, LGO is absent from final priorities,
  and the missing-symbol union fell from 19 to 18
- later 40-F amendments and adjusted values are excluded; evidence manifest
  SHA256:
  `e60a5c7d23ebe82dba0bbf9873f5c197bcbddd0c17c5bfbb3ee1672e0fedbc38`

### NCTY exact US-GAAP annual-plus-half-year TTM growth recovery

- `scripts/research_v14_ncty_h1_ttm_growth.py`
- `tests/test_research_v14_ncty_h1_ttm_growth.py`
- SHA-locked the audited FY2018/FY2019 20-F filings and the issuer's H1
  2018/H1 2019/H1 2020 6-K exhibits; verified ten exact statement rows for
  consolidated `Total net revenues` and `Net (loss) gain attributable to
  The9 Limited`
- every TTM operand uses the reported RMB column because each filing's USD
  column is only a convenience translation at a different period-end rate;
  no mixed-rate USD splice or FX estimate is used
- current TTM revenue is
  `341,495 - 251,327 + 465,726 = 555,894 RMB`; prior TTM is
  `17,431,858 - 10,460,086 + 251,327 = 7,223,099 RMB`, growth
  `-92.303940%`
- current TTM net income is
  `-177,795,168 - (-40,486,287) + 450,573,532 = 313,264,651 RMB`;
  prior TTM is
  `-217,092,926 - (-74,994,778) + (-40,486,287) = -182,584,435 RMB`,
  growth `+271.572484%` under the frozen absolute-denominator rule
- the final H1 source was public 30 days before the 2021-01-29 signal; the
  sole age150 observation resolves, but the negative revenue growth keeps
  NCTY outside the candidate set. NCTY is absent from final priorities and
  the missing-symbol union fell from 18 to 17
- the post-signal FY2020 20-F, USD convenience translations, adjusted values,
  and estimates are excluded; evidence manifest SHA256:
  `810c6097096e874265996b496bc1399c6807eb3ba51993edfa8d70d8fc691370`

### PLL exact IFRS annual-plus-half-year TTM loss recovery

- `scripts/research_v14_pll_exact_ttm_loss.py`
- `tests/test_research_v14_pll_exact_ttm_loss.py`
- SHA-locked the audited FY2020 20-F and the 2021-03-18 6-K Exhibit 99.1
  containing exact H1 FY2021/H1 FY2020 comparatives; verified the consolidated
  IFRS `Loss for the year/period` rows
- all three operands are presented in USD under a consistent consolidated IFRS
  basis; the interim report follows IAS 34 and explicitly retains the annual
  accounting policies
- current TTM net loss is
  `-5,671,099 - (-3,475,979) + (-5,465,327) = -7,660,447 USD`
- the final source was public 13 days before the 2021-03-31 signal; both the
  $2m and $10m age150 observations resolve through the frozen nonpositive-profit
  gate, PLL is absent from final priorities, and the missing-symbol union fell
  from 17 to 16
- no revenue or growth metric is invented; later US-GAAP redomiciliation
  filings and adjusted measures are excluded; evidence manifest SHA256:
  `e4722666d0fccb85677a0693eac1b844dbb150fa4e45e27ce50df2c5be7f9f3b`

### QFIN exact annual-plus-nine-month TTM growth recovery

- `scripts/research_v14_qfin_m9_ttm_growth.py`
- `tests/test_research_v14_qfin_m9_ttm_growth.py`
- reused the SHA-locked FY2019 20-F Company Facts payload and SHA-locked the
  issuer's 2019 and 2020 Q3 6-K exhibits; verified exact consolidated US-GAAP
  `Total net revenue` and `Net income` nine-month statement rows
- all operands remain in reported CNY thousands. Prior TTM revenue/net income
  are `8,385,430`/`2,666,337`; current TTM revenue/net income are
  `12,627,349`/`2,721,042`
- exact revenue growth is `+50.586780%`; exact net-income growth is
  `+2.051691%`
- the last source was public 70 days before the 2021-01-29 signal; both $2m
  and $10m age150 observations resolve, QFIN is absent from final priorities,
  and the missing-symbol union fell from 16 to 15
- the recovered net-income growth remains below the frozen 15% gate, so QFIN
  does not become eligible. Non-GAAP values, USD convenience translations,
  estimates, and the post-signal FY2020 20-F are excluded; evidence manifest
  SHA256:
  `460ab629fc99fd7c19877bfd1a9d8deec37aa0f7add8d7eba5d578891373f54b`

### RCEL exact audited IFRS annual loss recovery

- `scripts/research_v14_rcel_exact_annual_loss.py`
- `tests/test_research_v14_rcel_exact_annual_loss.py`
- SHA-locked the original 2019-10-31 FY2019 20-F and the 2019-11-19 20-F/A;
  the amendment expressly states that it only furnishes XBRL and does not
  amend or update the original financial information
- verified the audited consolidated IFRS `Loss for the period` row in reported
  Australian dollars: FY2019 `-35,160,227`, FY2018 `-16,519,155`, and FY2017
  `-11,511,024`
- emitted only the exact FY2019 `net_income_ttm` loss. The original 20-F was
  public 61 days before the 2019-12-31 signal, so the age150, age365, and
  age550 observations all resolve through the frozen nonpositive-profit gate
- RCEL is absent from final priorities and the missing-symbol union fell from
  15 to 14. Total comprehensive loss, manufactured revenue growth, non-GAAP
  values, and post-signal filings are excluded; evidence manifest SHA256:
  `140a2347f3ba446b41de1cdb74c93c00c73dd00ca3f307e7eaaa9de510a1c89e`

### STNE exact disclosed-precision 2020Q3 IFRS recovery

- `scripts/research_v14_stne_q3_2020.py`
- `tests/test_research_v14_stne_q3_2020.py`
- SHA-locked the original 2020-10-29 Q3 6-K Exhibit 99.1 and verified its
  `Statement of Profit or Loss (R$mm)` rather than the separate adjusted table
- added only the reported-precision 2020Q3 `Total revenue and income`
  `R$934.3m` and IFRS `Net income for the period` `R$249.1m`; comparative
  2020Q3 values and percentage columns are source-locked as row guards
- together with the existing first-available 2018Q4-2020Q2 quarters, prior TTM
  revenue/net income are `R$2,322.418m`/`R$667.302m`, and current TTM values
  are `R$3,101.359m`/`R$795.325m`; growth is `+33.540086%`/`+19.185167%`
- the Q3 filing was public 120 days before the 2021-02-26 signal; both $2m and
  $10m age150 observations resolve and both frozen growth gates pass. This is
  data repair evidence only: no training, promotion, or release was run
- STNE is absent from final priorities and the missing-symbol union fell from
  14 to 13. Added precision, adjusted income, estimates, and post-signal
  filings are excluded; evidence manifest SHA256:
  `1084f64a243e673f810d4e35df0b118f860d1c401fd6d47abe3cea3108364a67`

### ROAD homogeneous previous-GAAP TTM recovery

- `scripts/research_v14_road_previous_gaap_ttm_growth.py`
- `tests/test_research_v14_road_previous_gaap_ttm_growth.py`
- SHA-locked the ROAD Company Facts cache and the original 2019Q3 10-Q; the
  latter expressly discloses modified-retrospective ASC 606 adoption and exact
  nine-month values both as reported and without ASC 606
- used only the issuer's `Without Application of ASC 606` M9 2019 revenue
  `547.242m USD` and net income `26.655m USD`, together with exact audited
  FY2017/FY2018 and M9 2017/M9 2018 previous-GAAP operands
- homogeneous prior/current TTM revenue is `652.022m`/`762.943m`, growth
  `+17.011849%`; prior/current net income is `47.914m`/`41.799m`, growth
  `-12.762449%`
- the transition table was public 52 days before the 2019-09-30 signal; all
  three repeated age150/365/550 observations resolve, ROAD is absent from final
  priorities, and the missing-symbol union fell from 13 to 12
- the negative net-income growth fails the frozen 15% gate. As-reported ASC 606
  values, cross-basis splices, estimates, and post-signal filings are excluded;
  evidence manifest SHA256:
  `8ce67a5261abb2b9e67db74668968ed3212fde6c0590ca02ff4b43d3ce1b73a5`

### RCON exact annual-plus-half-year TTM loss recovery

- `scripts/research_v14_rcon_h1_ttm_loss.py`
- `tests/test_research_v14_rcon_h1_ttm_loss.py`
- SHA-locked the original FY2020 20-F and 2021H1 6-K Exhibit 99.1; verified
  the exact US-GAAP `Net loss attributable to Recon Technology, Ltd` rows
- kept every operand in the issuer's reported RMB columns: FY2020
  `-19,246,701`, H1 FY2020 `-6,701,197`, and H1 FY2021 `-8,935,652`
- current TTM is
  `-19,246,701 - (-6,701,197) + (-8,935,652) = -21,481,156 CNY`
- the half-year filing was public 53 days before the 2021-05-28 signal; the
  age150 observation resolves through the frozen nonpositive-profit gate,
  RCON is absent from final priorities, and the missing-symbol union fell from
  12 to 11
- emitted only the exact TTM loss. Revenue, growth, USD convenience
  translations, adjusted measures, and post-signal filings are excluded;
  evidence manifest SHA256:
  `2d0d5fa4fb64fdc5e857f84d2508372b313ab92bd58ec997709650ab0ef558d4`

### SBET historical-CIK accounting-acquirer loss recovery

- `scripts/research_v14_sbet_accounting_acquirer_loss.py`
- `tests/test_research_v14_sbet_accounting_acquirer_loss.py`
- rejected current CIK `1981535` for the 2021-08-31 signal and SHA-locked the
  original CIK `1025561` merger proxy plus completed-merger exhibit
- the proxy's audited Old SharpLink historical actuals report FY2020 US-GAAP
  consolidated net loss `-1,139,072 USD`; the separate pro-forma tables are
  explicitly excluded
- pre-signal transaction evidence identifies Old SharpLink as the legal
  acquiree/accounting acquirer; at close former SharpLink holders owned about
  86%, its directors and officers took control, and trading moved to `SBET`
- availability is conservatively set to the 2021-07-27 completed-merger filing,
  35 days before signal. All three age150/365/550 observations resolve through
  the frozen nonpositive-profit gate, and the missing-symbol union fell from
  11 to 10
- legacy MTS earnings, later domesticated-CIK facts, adjusted values, growth,
  and all pro-forma amounts are excluded; evidence manifest SHA256:
  `36af647c4d99b6646307089f53042cc46058e401a49949285e248e57917c4756`

### SOHU restated eight-quarter GAAP recovery

- `scripts/research_v14_sohu_restated_quarters.py`
- `tests/test_research_v14_sohu_restated_quarters.py`
- SHA-locked four pre-signal 6-K earnings exhibits filed from 2020-11-16
  through 2021-08-09 and verified the exact three-month revenue and
  `Net income/(loss) attributable to Sohu.com Limited` statement rows
- kept Sogou in the issuer's retrospectively presented discontinued-operations
  basis. The parent-attributable quarterly net-income series sums exactly to
  the audited FY2020/FY2019 values already used by the candidate; consolidated
  total income and continuing-only headline values are different bases and are
  excluded
- prior/current TTM revenue is `694.965m`/`837.624m USD`, growth
  `+20.527509%`; prior/current parent-attributable net income is
  `-140.050m`/`103.859m USD`, growth `+174.158515%`
- the final Q2 exhibit was public 22 days before the 2021-08-31 signal. The
  age150 observation resolves and passes both tested 15% audit thresholds as
  well as the canonical 25% profit/10% revenue execution gates; SOHU is absent
  from final priorities
- the live missing-symbol union fell from 10 to 8 because SOHU resolved and
  its passing observation displaced already-classified BCTX from the bounded
  candidate path. This is a candidate-membership change, not authorization to
  retrain or promote. Non-GAAP values, the later Sogou sale completion/gain,
  estimates, and post-signal filings are excluded; evidence manifest SHA256:
  `6ba38b1c7ce4d1c28cfbc88bd65cf32c52881c89877c97482d31a223b8bb8e94`

### ITOS zero-revenue-denominator negative evidence

- `scripts/research_v14_itos_zero_revenue_growth.py`
- `tests/test_research_v14_itos_zero_revenue_growth.py`
- SHA-locked the 2020Q3 10-Q, FY2020 10-K, and 2021Q3 10-Q under CIK
  `1808865`, including their exact acceptance times and the annual/nine-month
  net-income plus license-revenue statement rows
- exact FY-minus-M9 arithmetic gives Q4 2019 `-5.376m`, Q4 2020 `-14.904m`,
  prior TTM net income `-28.505m`, current TTM net income `14.745m`, and profit
  growth `+151.727767%`
- the same timely filing proves current TTM revenue `104.271m` and prior TTM
  revenue exactly `0`; revenue growth is therefore undefined. It must not be
  represented as zero, infinity, a fabricated denominator, or a passing
  growth percentage
- all three age150/365/550 observations at 2021-12-31 are source-locked as
  `unrecoverable_zero_revenue_denominator`; the package emits zero candidate
  rows and leaves formal financials unchanged
- generated evidence manifest:
  `output/research_only/v14/itos_zero_revenue_growth/manifest.json`, SHA256
  `e51a378175f715e553a0f78af379239012dc88bd3aeb49532f0911164b23b0dd`

### ARGX currency-boundary negative-evidence extension

- `scripts/research_v14_argx_quarterly_reports.py`
- `tests/test_research_v14_argx_quarterly_reports.py`
- retained the existing 32 accepted facts / 16 continuous EUR quarters for
  2017-2020; no shared candidate or formal financial file changed
- SHA-locked all 17 SEC source archives and verbatim-locked the Q1/H1 2021
  disclosures that functional/presentation currency changed from EUR to USD
  and comparative presentation was applied retrospectively under IAS 8/21
- by the 2021-08-31 signal, the issuer had filed exact 2021H1 USD
  (`470.398m` revenue, `63.167m` net income) and comparative 2020H1 USD
  (`24.683m`, `-226.590m`), but no exact full-year 2020 USD income statement
  existed to derive 2020H2; the prior TTM also lacked a homogeneous 2019 USD
  annual/H1 chain
- the later 2020 annual USD comparator (`41.243m`, `-608.455m`) was filed only
  on 2022-03-03. It yields current TTM (`486.958m`, `-318.698m`) but cannot be
  backdated; mixing the original EUR annual with USD half-years would instead
  manufacture (`482.140m`, `-239.166m`)
- both repeated age150 observations are bound as
  `unrecoverable_pre_signal_usd_ttm_chain_absent`; external/rounded FX
  conversion and late-comparator backdating are rejected
- evidence manifest:
  `output/research_only/v14/argx_sec_quarterly_reports_2019_2021/manifest.json`,
  SHA256
  `f5bbe77d87e39c3af9ba8656cb1fb6627b726f1027654c1794669b3798a13a53`

### NESR annual direct growth

- `scripts/research_v14_nesr_annual_growth.py`
- `tests/test_research_v14_nesr_annual_growth.py`
- recovered March, April, and June 2021 observations
- calendar 2020 versus 2019 Successor NESR: revenue `+26.695778%`,
  net income `+27.240626%`; official 20-F filed 2021-03-24

### CGEN direct TTM loss

- `scripts/research_v14_cgen_direct_ttm_loss.py`
- `tests/test_research_v14_cgen_direct_ttm_loss.py`
- exact IFRS consolidated TTM:
  `-27,337k - (-14,385k) + (-13,374k) = -26,326k USD`
- available 2020-07-30; 2020-08-31 becomes known nonpositive

### EVGN direct TTM loss

- `scripts/research_v14_evgn_direct_ttm_loss.py`
- `tests/test_research_v14_evgn_direct_ttm_loss.py`
- official 2020-11-18 6-K provides all comparable operands
- exact IFRS consolidated TTM:
  `-19,115k - (-12,423k) + (-17,443k) = -24,135k USD`
- 2021-01-29 becomes known nonpositive

### Pinned Nasdaq snapshot recovery

- `scripts/research_v14_recover_pinned_nasdaq_snapshot.py`
- `tests/test_research_v14_recover_pinned_nasdaq_snapshot.py`
- 2019-07-15 snapshot from commit
  `46844218084c6d03fad878f1197778587ff167c0f`
- source Git blob SHA `2b113f07b34152c3d3db7a4b7b625568d83ae486`
- payload SHA256
  `4d303e124f355ecd005f7179df8b8ef1b5ae498dc76cf3bc355810bfb487b7e3`
- 3,462 normalized rows; normalized SHA256
  `4140db4c38d67ef4abf8dad83a3102a826fe5fb76747b6c9bf3a445170add8f6`
- footer `File Creation Time: 0715201921:32`
- reduced the 2019-07-31 signal's snapshot age to 16 days

### HONE old/new CIK transition TTM growth

- `scripts/research_v14_hone_cik_transition_ttm.py`
- `tests/test_research_v14_hone_cik_transition_ttm.py`
- the 2019 second-step conversion split filings between old CIK 1668224 and
  new CIK 1769617; the ordinary ticker map alone missed this bridge
- three SHA-locked original XBRL instances prove FY2017/FY2018 and 9M
  2017/2018/2019 bank-income components
- all three 9M2018 comparatives match across the two registrants
- exact bank revenue TTM: `133.272m -> 162.003m USD` (`+21.5573%`)
- exact net-income TTM: `12.876m -> 14.072m USD` (`+9.2886%`)
- recovered both 2019-12-31 and 2020-01-31 observations

### SIBN direct TTM loss

- `scripts/research_v14_sibn_direct_ttm_loss.py`
- `tests/test_research_v14_sibn_direct_ttm_loss.py`
- SHA-locked Company Facts archive, original 2018Q3/FY2018/2019Q3 filings
- exact TTM: `-17.453m - (-12.140m) + (-29.305m) = -34.618m USD`
- recovered 2020-01-31 and 2020-02-28 as known nonpositive

### KNSA direct TTM loss

- `scripts/research_v14_knsa_direct_ttm_loss.py`
- `tests/test_research_v14_knsa_direct_ttm_loss.py`
- SHA-locked original 2018Q3/FY2018/2019Q3 US-GAAP facts
- exact TTM: `-103.227m - (-60.647m) + (-130.070m) = -172.650m USD`
- recovered 2020-02-28 as known nonpositive

### HCAT direct TTM loss

- `scripts/research_v14_hcat_direct_ttm_loss.py`
- `tests/test_research_v14_hcat_direct_ttm_loss.py`
- two SHA-locked original SEC XBRL instances; no mutable current snapshot used
- exact TTM: `-60.096m - (-45.830m) + (-71.999m) = -86.265m USD`
- recovered 2021-01-29 as known nonpositive

### WPRT direct TTM loss

- `scripts/research_v14_wprt_direct_ttm_loss.py`
- `tests/test_research_v14_wprt_direct_ttm_loss.py`
- SHA-locked FY2019 40-F XBRL, 2020Q3 interim financial statements, and
  contemporaneous earnings release
- exact TTM: `0.041m - (-0.610m) + (-11.477m) = -10.826m USD`
- adjusted EBITDA is explicitly identified and excluded
- recovered the 2020-11-30 observation in all three age variants

### VFF direct TTM loss

- `scripts/research_v14_vff_direct_ttm_loss.py`
- `tests/test_research_v14_vff_direct_ttm_loss.py`
- two SHA-locked original SEC XBRL instances
- exact TTM: `2.325m - 9.509m + 4.591m = -2.593m USD`
- recovered the 2020-11-30 age150 observation as known nonpositive; the longer
  age scenarios were already resolved by older eligible evidence

### SY exact TTM growth

- `scripts/research_v14_sy_exact_ttm_growth.py`
- `tests/test_research_v14_sy_exact_ttm_growth.py`
- five SHA-locked original SEC filings/exhibits, all RMB US-GAAP; convenience
  USD translations and non-GAAP measures are rejected
- as of 2020-05-18, exact revenue TTM growth is
  `1,128,138k / 640,266k - 1 = 76.1983%` and net-income TTM growth is
  `94,936k / 60,175k - 1 = 57.7665%`
- as of 2020-11-25, exact revenue TTM growth is
  `1,228,527k / 976,476k - 1 = 25.8123%` and net-income TTM growth is
  `37,105k / 147,592k - 1 = -74.8597%`
- inserted eight strict quarterly facts and resolved the 2020-07-31 and
  2021-02-26 observations across all three $2m-liquidity age scenarios, six
  aggregate gaps; the later observation is complete but fails profit growth

### GLPG Q3 exact TTM growth extension

- `scripts/research_v14_glpg_direct_ttm_loss.py`
- `tests/test_research_v14_glpg_direct_ttm_loss.py`
- upgraded the existing half-year loss package to four SHA-locked original SEC
  sources: FY2018 and H1 2019 XBRL plus the Q3 2018 and Q3 2019 exhibits
- retained the existing `-29.259m` and `-66.108m EUR` direct TTM losses
- exact Q3 revenue TTM is `865.245m` versus `254.618m EUR`, growth `239.8208%`
- exact Q3 net-income TTM is `280.241m` versus `-74.004m EUR`, growth
  `478.6836%`; the selector uses the prior absolute loss as its denominator
- resolved 2019-12-31 and 2020-01-31 in both age150 liquidity scenarios, four
  aggregate gaps, and converted later observations from known-nonpositive to
  genuine growth passes

### HCM six-month-cadence negative-evidence extension

- `scripts/research_v14_hcm_direct_ttm_loss.py`
- `tests/test_research_v14_hcm_direct_ttm_loss.py`
- retained four exact annual/H1 TTM losses; no new candidate identity rows were
  inserted and the SY+GLPG candidate remains the latest reproducible candidate
- source-locked the immutable 2020-Q3, 2020-Q4, and 2021-Q1 SEC quarterly master
  indexes and all 22 nonfinancial 6-K primary documents from 2020-07-30 through
  the 2021-01-29 PIT cutoff
- the last exact 2020-H1 TTM loss, `-110.349m USD`, was filed 2020-07-30 and
  was 154/183 days old at the 2020-12-31 and 2021-01-29 signals, exceeding the
  age-150 limit
- FY2020 loss `-125.730m USD` was filed only 2021-03-04; backdating it or
  splitting H2 into an invented quarter is rejected
- both remaining HCM observations are now source-exhausted and explicitly
  `unrecoverable_six_month_reporting_cadence`; this is classification, not a
  synthetic data fill, so the six-scenario numeric missing count stayed 88 at
  the HCM checkpoint
- evidence manifest:
  `output/research_only/v14/hcm_direct_ttm_loss/manifest.json`, SHA256
  `204b76224f03e54a2d33f40b39f1aeed9929029a1e69ac8149881f99149e5b90`

### RLMD fiscal-transition exact TTM loss

- `scripts/research_v14_rlmd_transition_ttm_loss.py`
- `tests/test_research_v14_rlmd_transition_ttm_loss.py`
- three SHA-locked SEC XBRL instances: FY2019 10-K, the six-month 2019 10-KT
  transition report, and 2020-Q1 10-Q; all operands are exact USD US-GAAP
  `NetIncomeLoss`
- exact calendar-2019 bridge:
  `-17.318060m - (-10.509403m) + (-8.196542m) = -15.005199m USD`
- exact 2020-Q1 TTM:
  `-15.005199m - (-2.686065m) + (-10.673316m) = -22.992450m USD`
- the last source was filed 2020-05-15, so the 2020-05-29 and 2020-06-30
  signals are only 14/46 days old; both become known-nonpositive
- inserted one strict identity row, resolving both RLMD observations; aggregate
  missing observations fell `88 -> 86`, unique missing symbols `27 -> 26`, and
  known-nonpositive observations rose `1,819 -> 1,821`
- evidence manifest SHA256:
  `b8b7a224db7b8a24bbc0da7e05648c5ff67044a8daf1676f6ac1fd4ebcbcbd48`

### SMPL predecessor/successor negative evidence

- `scripts/research_v14_smpl_acquisition_basis_gap.py`
- `tests/test_research_v14_smpl_acquisition_basis_gap.py`
- three SHA-locked original SEC XBRL instances: fiscal-2017 10-K,
  fiscal-2018 10-K, and fiscal-2019 Q1 10-Q; 22 exact USD US-GAAP facts are
  checked, including the explicit `us-gaap:PredecessorMember` dimension
- a valid successor-only current TTM was public on 2019-01-03:
  revenue `431.429m - 106.587m + 120.931m = 445.773m USD`; net income
  `70.455m - 10.218m + 15.257m = 75.494m USD`
- the required prior TTM necessarily crosses the 2017-07-06/2017-07-07
  predecessor/successor boundary. The rejected arithmetic would create
  revenue `402.955m` and net income `1.396m USD`, implying apparently passing
  growth of `10.6260%` and `5,307.8797%`; it is contamination, not evidence
- the alternative acquisition figures are explicitly unaudited pro forma and
  the reported fiscal-2017 pro-forma net income itself changed from `28.701m`
  in the 2017 filing to `28.857m USD` in the 2018 filing. Quarterly YoY
  substitution and the post-signal 2019-04-04 Q2 filing are also rejected
- all six scenario-observations (two signals repeated at age150/365/550) are
  source-locked as `unrecoverable_predecessor_successor_basis_split`; zero
  candidate rows were created, so the numeric audit count correctly remains 86
- evidence manifest:
  `output/research_only/v14/smpl_acquisition_basis_gap/manifest.json`, SHA256
  `e54d2eacbbb9e4604318444cec109af46fa1cdcfa3e50a41cdd3c0436743a563`

### ADUS re-audit/comparator negative-evidence upgrade

- `scripts/research_v14_adus_pit_unrecoverable.py`
- `tests/test_research_v14_adus_pit_unrecoverable.py`
- retained six SHA-locked SEC documents and the raw Company Facts cutoff;
  validates 29 exact USD operands and 11 disclosure fragments
- the 2020-03-17 issuer release labeled FY2019 results preliminary and said
  2017/2018 required re-audit; the 2020-05-04 Q1 release remained preliminary,
  gave revenue only as rounded `$190.2m`, and lacked a reliable same-basis Q1
  comparator
- revised exact FY2018 and Q1-2019 comparators were filed only on 2020-08-10,
  after both 2020-05-29 and 2020-07-31 signals; backdating them is rejected
- all three repeated age150 observations are now bound to the current audit as
  `unrecoverable_reaudit_comparator_not_available`; zero facts are emitted
- removed cache-hit/download-state nondeterminism from the evidence manifest;
  consecutive cached builds are byte-identical
- evidence manifest:
  `output/research_only/v14/adus_pit_unrecoverable/manifest.json`, SHA256
  `8603b3fd6b64abcde18eaa2a9413f69d036ca0b95f8f6499b6ad901c88e2c994`

### GGAL IAS-29 negative-evidence extension

- `scripts/research_v14_ggal_ias29_quarters.py`
- `tests/test_research_v14_ggal_ias29_quarters.py`
- retained the existing 16 strict IAS-29 ARS facts that resolve the 2021
  signals; those facts remain available only from 2021-08-31 and are not
  backdated into 2019
- added SHA locks for the original 2018 20-F, the 2019Q1 result exhibit, and
  the 2019Q2 result exhibit. The 20-F states that 2018/2017 operations are in
  December-2018 purchasing power under IAS 29; Q1 and Q2 both explicitly say
  IAS-29 restatement was not used and would have widespread effects
- Q2 was filed only on 2019-08-14, after the 2019-06-28 and 2019-07-31
  signals. It is both late and still nominal, so it cannot repair either signal
- all 12 repeated observations across six age/liquidity scenarios are bound as
  `unrecoverable_ias29_measurement_basis_conflict`; CPI-ratio estimates,
  nominal/restated mixing, and backdating the Q2 report are rejected
- no new candidate facts were created for 2019, so the numeric audit correctly
  remains unchanged
- evidence manifest:
  `output/research_only/v14/ggal_ias29_quarters_2019q3_2021q2/manifest.json`,
  SHA256
  `7432407d801483a598e579d6473c9a46dfedcdd122aa89e31f6c0401838aad58`

Earlier packages in the chain include AFYA, LEGN, SDGR, ALLT, GLNG, ALLK,
ASND, CSIQ, CRON, IQ, JAMF, LX, IIIV, PERI, UXIN, GAIN, AZPN, LI, GILT,
ESLT, MESO, MMYT, OPRA, MOGO, ICLK, AUDC, XP, MEOH, and RGLD. Repository
scripts/tests are the source of truth; do not hand-edit candidate CSVs.

## 5. Final six-scenario path audit

| Scenario | Missing observations | Known nonpositive | Missing symbols |
| --- | ---: | ---: | ---: |
| liq10m / age150 | 6 | 172 | 5 |
| liq10m / age365 | 4 | 180 | 3 |
| liq10m / age550 | 4 | 180 | 3 |
| liq2m / age150 | 12 | 357 | 8 |
| liq2m / age365 | 7 | 364 | 5 |
| liq2m / age550 | 7 | 364 | 5 |

Aggregate across six scenarios:

- missing financial observations: **40**, down from **174** this continuation
- unique missing symbols: **8**, down from **42**
- known-nonpositive observations: **1,617**; the count is path-dependent and
  fell as repaired positive observations changed bounded candidate membership
- unresolved observable price competitors: **45**; this is still blocked by
  stale universe dates and price-path evidence, not by the now-classified
  financial gaps alone

The aggregate repeats ticker/dates used by multiple configurations; it is a
path-audit workload measure, not a unique-report count.

Gates:

- `research_pretraining_allowed=true`
- `adaptive_training_eligible=false`
- `all_candidate_financial_scenarios_complete=false`
- `historical_member_prices_complete=false`
- `signal_membership_snapshots_complete=false`

Fixed-grid diagnostics are allowed, but final adaptive training is not yet a
valid claim while the candidate membership/data path can still change.

## 6. Universe and ordinary-share status

Use dated exchange snapshots, not only the current list. Security-type logic
must exclude warrants, units, rights, test issues, and other non-common-equity
instruments at the applicable historical date. A current Nasdaq list can expire
and must not be projected backward.

After importing 2019-07-15:

- maximum observed snapshot age: **98 days**; limit: **30 days**
- stale dates: `2019-03-29`, `2019-04-30`, `2019-05-31`, `2019-08-30`,
  `2019-09-30`

The immutable-archive search is now source-exhausted for these exact dates:

- `scripts/research_v14_universe_gap_negative_evidence.py`
- `tests/test_research_v14_universe_gap_negative_evidence.py`
- the SHA-bound Common Crawl catalog checked all 64 indexes spanning 2015-2020,
  found 36 captures overall, zero 2019 captures, and had no query errors
- the SHA-bound broad GitHub catalog examined 153 search results, retained 122
  valid dated Nasdaq files, and found 2019 captures only on 01-09, 02-22,
  10-02, 11-06, 12-27, and 12-31
- ten additional exact searches (five dates times `nasdaqlisted.txt` and
  `nasdaqtraded.txt`) all returned zero results; the separately pinned
  2019-07-15 snapshot remains bound at 3,462 rows
- classification is `SOURCE_EXHAUSTED_EXCLUDE_SIGNAL_DATES`: skip exactly the
  five stale scheduled signals and carry the prior holdings forward. Never
  backdate a later snapshot, raise the 30-day limit, or fabricate membership
- `calculate_can_slim_returns`, its ledger variant, scheduled replay, all
  candidate replays, continuous walk-forward, and 10/30/50bps stress now share
  the same exact exclusion set; a deterministic test proves the skipped signal
  does not call the universe resolver or force turnover
- generated negative-evidence SHA256:
  `1292eed89c98af4394255095f5de6ae848a662fa36e6fa6d22e94e24e3b1e734`

Relevant tooling:

- `scripts/research_v14_github_universe_audit.py`
- `scripts/research_v14_import_github_universe.py`
- `scripts/research_v14_commoncrawl_universe_audit.py`
- `scripts/research_v14_recover_commoncrawl_universe.py`
- `scripts/research_v14_recover_pinned_nasdaq_snapshot.py`
- `scripts/research_v14_universe_gap_negative_evidence.py`

## 7. Strategy evidence and limits

Old v4 Candidate 15 was Top 10, 10% single-name cap, $10 minimum price, $10m
minimum median dollar volume, 150-day financial age, monthly rebalance, and
10bps baseline cost. Its stale diagnostic was 4/5 years beating Nasdaq and 4/5
at 10/30/50bps, about 19.26% median excess, -32.56% worst annual excess,
-23.92% maximum drawdown, and 93.16% time underwater. A 2024 SEZL event was
material, although zeroing the largest event left about 13.95% 2024 excess.

These are in-sample diagnostics selected after viewing history, not independent
validation or proof of live fitness. Do not revive the older Top 3 or any `6/6
validated` wording.

Final fixed-18 regression diagnostic:

`output/research_only/v14/adaptive_pretrain_v14_sy_glpg_rlmd_20260827_no_tracking/`

- 18 candidates; 5 calendar evaluation years
- 3/5 wins versus Nasdaq at each of 10/30/50bps
- shadow IDs `[3,0,9]`
- `parameters_frozen=false`, `release_status=BLOCKED`,
  `promotion_eligible=false`
- quarterly input SHA256:
  `9f74c1ec72116572118ca3e6be6106fc408abeb69db3acc64aee6fb35359f323`
- audit input SHA256:
  `afe7b2867c6eafa7c7a2d4cb9af433224cadd56239f6f5048f6e0f92dabb9c27`
- price-overlay content-manifest SHA256:
  `1c18b8c3cc44d407086b5fc15eb0b3ee208f855057d65c86377d92f85aadc4a8`
- 422 bound universe snapshot files
- walk-forward CSV SHA256:
  `d605335df246ec3e6dccf0c2ea733ec6fe5d2700d26c0a383661a4e18a874c00`
- summary SHA256:
  `846fd54e4dfd2202602e868f8c09263a6393ab77fdff68f35eb5855c8b446b15`
- candidate annual-results CSV SHA256:
  `78a12c7046d2903d20a44564131b7403cd37a2d0acd61175cacaf86ac374d646`

Annual diagnostic sequence and result:

| Year | Config IDs | Strategy | Nasdaq | Excess |
| --- | --- | ---: | ---: | ---: |
| 2022 | `[0,15,12]` | -20.8994% | -33.1000% | +12.2007% |
| 2023 | `[15,12]` | +9.7960% | +43.4231% | -33.6271% |
| 2024 | `[17,0,14]` | +120.7972% | +28.6412% | +92.1559% |
| 2025 | `[0,3]` | -2.2625% | +20.3575% | -22.6200% |
| 2026 | `[3,0,9]` | +22.4816% | +9.8023% | +12.6793% |

GLPG materially changed the historical candidate results and the selected
2023/2025 ensembles. Versus the preceding SY-only checkpoint, 2023 strategy
return fell from `20.3187%` to `9.7960%` and 2025 fell from `5.2746%` to
`-2.2625%`. The total remains 3/5 at 10/30/50bps and the current shadow IDs
remain `[3,0,9]`, but the adverse result must be retained. This is direct
evidence that incomplete PIT data was affecting the diagnostic and that the
candidate path is not stable. It cannot unlock adaptive training or live
trading.

The RLMD checkpoint changes only the bound quarterly/audit inputs. Its
`candidate_annual_results.csv` and `walk_forward_annual.csv` are byte-identical
to the GLPG checkpoint, including all annual returns, selected config IDs, and
3/5 cost-stress result. This is useful coverage improvement, not additional
strategy evidence.

## 8. Remaining financial work

Latest age150/$2m symbols:

`ADUS, ARGX, GGAL, HCM, ITOS, MOMO, OZK, SMPL`

Use the generated live order, not this static list:

`output/research_only/v14/checkpoint_20260829_sohu_restated_quarters_recovered_financial_priorities.csv`

Highest-count age150 cases are ADUS, GGAL, HCM, and SMPL with two signals each.
All eight live symbols are now rigorously classified rather than safely
fillable; BCTX is also classified but was displaced from the bounded candidate
path after SOHU became complete. NCTY's single fact-producing gap and PLL's
two fact-producing observations, QFIN's two fact-producing observations,
RCEL's three repeated observations, STNE's two observations, ROAD's three
repeated observations, RCON's single observation, SBET's three repeated
observations, and SOHU's single observation are now resolved. Do not keep
searching merely to drive the numeric count to zero: the remaining records are
explicit negative states under the admissible source/accounting policy.

Known classifications/guardrails:

- ADUS May/July 2020: exact revised comparators were not public until
  2020-08-10. The three repeated age150 observations are source-exhausted; do
  not combine preliminary current values with unreliable pre-re-audit history.
- HCM Dec 2020/Jan 2021: exhaustive SEC index and 6-K evidence proves a
  six-month reporting-cadence gap; do not backdate the 2021-03-04 20-F.
- BCTX 2021-08-31: exact CAD net-income TTM, net-income growth, and zero revenue
  TTM were public by 2021-06-30. Revenue growth is nevertheless undefined
  because the current and prior revenue denominators are both zero. Do not
  manufacture `0%` or project the 2021-11-16 USD annual filing backward.
- RLMD May/June 2020 is resolved: the timely 10-KT plus Q1 10-Q can be bridged
  exactly; do not revert to the earlier assumption that calendar-2019 data was
  available only in 2021.
- MOMO: USD-to-RMB switch lacks a safe pre-signal 9M2017 RMB restatement. Its
  four SEC sources are SHA-locked and the generated zero-fact manifest has
  SHA256
  `4bebcb68bebfa53db55e134bc7cda1eb85b555a47c53b7173958cb7e30f53c10`.
- GGAL Jun/Jul 2019: the 2018 annuals are IAS-29 restated while Q1 is
  explicitly nominal; Q2 arrived 2019-08-14 and is also nominal. All 12
  repeated observations are source-exhausted; do not use CPI estimates to
  manufacture a homogeneous chain.
- DKNG is resolved: missing 2018 history prevents a growth bundle, but the
  timely audited-actual FY2019/H1 bridge proves a `-315.184m USD` current TTM
  net loss, so growth is unnecessary for the frozen positive-profit gate.
- FORR is resolved on a homogeneous ASC 605 basis: use the 2018Q3 issuer
  transition table's exact previous-guidance M9 values, never the as-reported
  ASC 606 M9 values or a cross-basis splice.
- GCMG is resolved: the pre-signal S-1 establishes recapitalization continuity
  and exact predecessor-parent attributable TTM loss `-2.355m USD`; do not
  substitute its positive consolidated income, pro-forma, or adjusted income.
- HIMX is resolved: the audited annual plus exact Q3 nine-month bridges produce
  a complete four-field TTM bundle on the consolidated IFRS `ProfitLoss` basis.
  Keep the incomplete-interim-disclosure warning and never substitute the
  separate non-IFRS or stockholder-attributable lines.
- HRMY is resolved: the audited annual plus exact Q3 nine-month bridges produce
  a complete four-field US-GAAP bundle. Preserve the reported M9 2019 revenue
  zero and use consolidated net income, not common-stockholder loss.
- HRZN is resolved: use audited BDC total investment income as the operating
  top line and consolidated net increase in net assets from operations as the
  GAAP bottom line. Do not revert to the generic no-revenue classification or
  substitute net investment income.
- LAKE is resolved: the timely FY2019 10-K did report `Net sales`; combine it
  with the two exact Q3 nine-month comparisons. Do not backfill the later
  FY2020 10-K or treat the old parser's missing revenue row as source absence.
- LGO is resolved: the pre-listing Form 40-F registration incorporated audited
  FY2020/FY2019 statements, with both years presented in USD. Preserve the
  disclosed presentation/functional-currency distinction and do not use later
  40-F amendments.
- NCTY is resolved: use exact FY-minus-H1-plus-H1 arithmetic in the issuer's
  reported RMB columns. The filing-specific USD columns are convenience
  translations at different exchange rates and must not be spliced. The
  recovered revenue growth is negative, so this removes missingness without
  making NCTY eligible.
- PLL is resolved: the audited FY2020 20-F and exact H1 FY2021/H1 FY2020 6-K
  comparatives prove a `-7.660447m USD` TTM loss by 2021-03-18. Emit only the
  nonpositive-profit fact; do not back-project later US-GAAP redomiciliation
  filings or invent revenue and growth metrics.
- QFIN is resolved: use the audited FY2019 annual values and exact 2019/2020
  Q3 nine-month comparatives in reported CNY to derive a complete TTM growth
  bundle. Do not substitute non-GAAP rows, mix in convenience USD columns, or
  backdate the FY2020 20-F. Its `+2.051691%` net-income growth fails the frozen
  15% gate, so this is completeness repair rather than candidate promotion.
- RCEL is resolved: the original FY2019 20-F proves an audited consolidated
  IFRS loss of `A$35.160227m`, and its later pre-signal amendment explicitly
  added only XBRL without changing the financial information. Emit only the
  negative profit state; do not substitute total comprehensive loss or invent
  growth metrics.
- STNE is resolved: retain the issuer's disclosed R$0.1m precision for 2020Q3
  total revenue and income and IFRS net income. The resulting TTM growth bundle
  passes both frozen growth gates, but this does not authorize retraining or
  promotion while other financial gaps remain. Never substitute adjusted net
  income or manufacture more precise values from rounded tables.
- ROAD is resolved on a homogeneous previous-GAAP basis: use the original
  2019Q3 10-Q transition table's exact without-ASC-606 M9 values, never its
  as-reported ASC 606 values or a cross-basis splice. The resulting revenue
  growth is positive but net-income growth is `-12.762449%`, so the evidence
  removes missingness without making ROAD eligible.
- RCON is resolved: use the exact audited FY2020 and H1 FY2020/H1 FY2021 RMB
  parent-attributable losses to derive the timely `-21.481156m CNY` TTM loss.
  Do not substitute convenience USD translations, consolidated loss including
  non-controlling interests, adjusted values, or post-signal filings.
- SBET is resolved against historical CIK `1025561`: Old SharpLink was the
  accounting acquirer and its audited FY2020 actual was a `-1.139072m USD`
  loss. Use the completed-merger date for availability. Do not project current
  CIK `1981535` backward or substitute legacy MTS/pro-forma earnings.
- SOHU is resolved with eight exact restated GAAP quarters from four pre-signal
  6-K exhibits. Use parent-attributable net income because it exactly matches
  the audited annual candidate series; do not mix consolidated total income,
  continuing-only headline values, non-GAAP values, or the later Sogou sale
  gain. Its positive growth changes candidate membership but does not authorize
  adaptive training before the protocol is frozen.
- OZK: seven exact source-locked quarters from 2018Q1 through 2019Q3 were
  public by the 2019-12-31 signal, one short of the eight-quarter growth chain;
  2019Q4 arrived only on 2020-01-16. This is a source-locked PIT history limit;
  mutable/unversioned FDIC output is not admissible historical evidence.
- SMPL Feb/Mar 2019: current successor-only TTM is exact and timely, but the
  prior TTM necessarily crosses the predecessor/successor acquisition basis;
  the only alternative is unaudited pro forma. The six repeated observations
  are source-exhausted; do not fill them.
- ARGX: both age150 observations are source-exhausted. H1 2020/2021 USD is
  available, but the exact 2020 full-year USD operand arrived only on
  2022-03-03 and the prior TTM lacks a homogeneous 2019 USD chain. Do not mix
  EUR annuals with USD half-years or estimate the conversion.
- ITOS 2021-12-31: pre-signal TTM net income is `14.745m` versus `-28.505m`,
  but collaboration revenue is `104.271m` versus an exact zero comparison
  base. The three repeated observations are source-locked as
  `unrecoverable_zero_revenue_denominator`; do not manufacture a percentage-
  growth denominator merely to clear the observation.

All eight current live symbols now have SHA-bound evidence manifests and tests,
including the paired OZK archive/residual packages. Never use FX estimates,
adjusted earnings, pro-forma values, later revisions, or cross-basis arithmetic
merely to reduce the numeric count.

## 9. Training and shorter-forward design

The research protocol is now frozen, but its one-shot final-data replay has not
yet executed:

- `scripts/research_v14_freeze_protocol.py`
- `tests/test_research_v14_freeze_protocol.py`
- `scripts/research_v14_frozen_replay.py`
- `tests/test_research_v14_frozen_replay.py`
- generated protocol:
  `output/research_only/v14/frozen_protocol_20260829.json`, SHA256
  `bf0e7440bce078e754443132fa02f5552a70f93c2bcf2db8dce660a66eda57b4`
- all 18 configurations are fixed: monthly, quarterly fundamentals, no adaptive
  channel, Top 3/5/10, $2m/$10m median dollar volume, and 150/365/550-day
  financial age. Canonical execution gates are 25% profit growth and 10%
  revenue growth; transaction-cost stresses are 10/30/50bps
- selection is fixed to 36-month rolling plus expanding history from 2019,
  annual updates from 2022, three-model rank weights `3/2/1`, grouping by
  Top-N/liquidity, and no no-evidence fallback
- exact ties now end with ascending `config_id` after combined rank, worst
  annual excess, and rolling quality, making selection independent of mapping
  insertion order
- 422 universe snapshots bind to content-manifest SHA256
  `b68c87a12027eb68420d61410bbdad5667fe9e2541b0be962975b98f434f39f4`;
  4,403 price files bind to
  `a8b944e74470afb377c22a7b533ddf1207397d3a285bd8ab0ca548235c13c715`
- the protocol also binds the candidate annual/quarterly/manifest, final audit
  and priority CSVs, formal financial invariants, five execution code files,
  the universe negative evidence, and all eight live financial classifications
- predeclared gates require at least 4/5 positive annual excess years at 10bps,
  at least 3/5 at 30bps and 50bps, positive compounded excess at every cost,
  maximum 10bps drawdown no worse than 40% absolute or Nasdaq by more than five
  percentage points, and positive compounded excess after removing the largest
  single-name net daily-return contributor while leaving its weight in cash
- a failure leaves `release_status=BLOCKED` and does not permit retuning the
  grid or gates. The manifest records `final_data_replay_executed=false` and
  `results_inspected=false`

The 2025-01-01 through 2026-07-17 interval is explicitly labeled
`HUMAN_EXPOSURE_CONTAMINATED`: earlier diagnostics already exposed these years,
so even a one-shot frozen-data replay is historical confirmation, not a clean
statistical holdout. Only a later 3-6 month future-forward phase can be genuinely
untouched. That short phase can test operational correctness, signal stability,
slippage, turnover, freshness, and kill switches, but cannot by itself prove
long-horizon alpha.

### Frozen one-shot historical replay result

The committed protocol was executed exactly once after local, `origin/master`,
and remote `master` converged at
`06da94f52949ec0ea82f740c769120ee9a597b77`. No code, inputs, grid, exclusion,
threshold, or removal method changed between freeze and execution.

- result manifest:
  `output/research_only/v14/frozen_replay_20260829/manifest.json`, SHA256
  `19db71cfc9d2c7c79257561de901b742be193a1fd15ae593a544f420d42602c8`
- protocol binding SHA256:
  `bf0e7440bce078e754443132fa02f5552a70f93c2bcf2db8dce660a66eda57b4`
- exact target-weight replay reconciled the emitted strategy, benchmark,
  invested fraction, turnover, and holdings columns at absolute tolerance
  `1e-12` before any gate was evaluated
- 10bps annual excess was positive in 2022, 2024, and partial-2026, but negative
  in 2023 (`-13.6785pp`) and 2025 (`-8.4354pp`). The resulting `3/5` win count
  fails the predeclared `4/5` requirement
- 30bps and 50bps each achieved the required `3/5` annual wins. Compounded
  strategy/Nasdaq returns were `+270.2698%`/`+63.1210%` at 10bps,
  `+229.8588%`/`+63.1210%` at 30bps, and `+193.8463%`/`+63.1210%` at 50bps;
  all three compounded-excess gates passed
- strategy maximum drawdown was `-30.1246%` versus Nasdaq `-35.4928%`; both the
  40% absolute drawdown limit and the five-percentage-point relative limit
  passed
- APP was the largest net arithmetic daily-return contributor at `+33.2198pp`.
  Removing APP at every frozen target and leaving its weight in cash produced
  `+178.7947%` strategy return versus `+63.1210%` Nasdaq, so the removal gate
  passed with `+115.6736pp` compounded excess
- the selector's code-bound `ensemble_size=3` is a cap: the 2023 stability
  shortlist contained only two configurations (`[3, 15]`), while the other
  annual snapshots used three. This is the frozen selector's pre-existing
  behavior, not a post-result change
- because one predeclared gate failed, `historical_gate_status=BLOCKED`,
  `release_status=BLOCKED`, and `promotion_eligible=false`. Do not rerun,
  weaken the 4/5 gate, remove 2023/2025, or start the future-forward promotion
  phase for this protocol

### Post-v14 diagnosis and separately frozen v16 hypothesis

The generic frozen-target attribution helper previously replayed prior account
state correctly but accumulated ticker contributions outside a requested
subwindow. `src/research/can_slim.py` now bounds contribution accumulation to
the normalized `start`/`end` interval while retaining earlier holdings state.
The new regression proves that a requested annual attribution excludes earlier
returns. This does not change v14's full-period result because v14 requested the
entire frozen replay interval.

The corrected diagnosis found that v14's relative shortfall was mainly the
market-regime filter leaving the portfolio entirely in cash during strong
Nasdaq rebounds, rather than a single-name collapse. In 2022 the average
invested fraction was only `7.97%`, but cash avoided a large benchmark decline;
in 2023 the strategy was in cash for 20 sessions and missed about `+10.35%` of
arithmetic Nasdaq return on those sessions; in 2025 it was in cash for 42
sessions and missed about `+11.56%`.

Two separately named, development-only hypotheses preserve every frozen v14
stock target and never modify formal data:

- v15 unconditionally filled residual cash with QQQ. Its manifest is
  `output/research_only/v15/benchmark_core_development/manifest.json`, SHA256
  `a9236a0e897e5e487eddbc8d2e022dd49c844015bb24f38f2261c11947cf6118`.
  It repaired 2023 at 10bps, but failed the 2022, drawdown, and 30/50bps annual
  gates. `all_development_gates_passed=false`; do not retune or confirm v15.
- v16 permits QQQ only while v14 is in cash and the prior QQQ session close is
  above a moving average. Its development candidate set was predeclared as
  20/50/100/200 sessions and bounded to 2022-01-01 through 2024-12-31. The
  manifest is
  `output/research_only/v16/trend_confirmed_qqq_development/manifest.json`,
  SHA256
  `42e59bc3070003e9efddb6fdab4a0a45e10d09e52f3ff21efc05ee49f212245c`.
  Only 50 sessions passed every development gate. At 10bps, annual excess was
  `+3.7563pp` in 2022, `-7.0972pp` in 2023, and `+123.1068pp` in 2024; the 2023
  excess improved `+6.5813pp` from v14. All three cost levels won 2/3 years,
  and 10bps maximum drawdown was `-30.3312%` versus Nasdaq `-35.4928%`.

The v16 historical-confirmation protocol was frozen before execution:

- generator: `scripts/research_v16_freeze_protocol.py`
- one-shot runner: `scripts/research_v16_frozen_confirmation.py`
- generated protocol:
  `output/research_only/v16/frozen_protocol_20260829.json`, SHA256
  `0d1e2dbb030a3bdb685290b6d93e9de64e797dae0dafa09dc47dc3eb648f922e`
- parameter: 50 QQQ sessions; observe only the prior-session close; activate
  only inside frozen v14 cash intervals; preserve all stock weights and leave
  any unallocated weight in cash
- confirmation interval: 2025-01-01 through 2026-07-17, explicitly
  `HUMAN_EXPOSURE_CONTAMINATED`, not statistically untouched
- confirmation gates: 10bps must beat Nasdaq in both confirmation years;
  30/50bps must each win at least 1/2; confirmation compounded excess must be
  positive at every cost
- full-history gates: at least 4/5 wins at 10bps and 3/5 at 30/50bps; positive
  compounded excess at every cost; 10bps drawdown no worse than 40% absolute
  or Nasdaq by more than five percentage points; positive compounded excess
  after removing the largest contributor and leaving its weight in cash
- the protocol records `historical_confirmation_executed=false` and
  `confirmation_results_inspected=false`. The frozen code was committed and
  pushed as `7f05572a32de6bb87ce84877caf4df2234a5ff0a`, with local,
  `origin/master`, and remote `master` identical before the one-shot run.

### Frozen v16 one-shot historical confirmation result

The v16 runner executed exactly once after the Git convergence above. It
regenerated and matched the frozen protocol, refused overwrite semantics, and
reconciled every 2022-2024 `strategy`, `benchmark`, `invested`, `turnover`, and
`holdings` value at 10/30/50bps to absolute tolerance `1e-12` before inspecting
confirmation results.

- result manifest:
  `output/research_only/v16/frozen_confirmation_20260829/manifest.json`, SHA256
  `ec50967c881de5f0ecb81e78f0f33fcaf538341aa3b8031e1a40f609b03788ec`
- the single failure was the predeclared 10bps annual-win requirement. 2025
  strategy/Nasdaq returns were `+19.5544%`/`+20.3575%`, an excess of
  `-0.8031pp`; partial-2026 was `+36.1309%`/`+9.8023%`, an excess of
  `+26.3286pp`. Therefore confirmation was 1/2 versus required 2/2 and full
  history was 3/5 versus required 4/5
- v16 materially narrowed v14's 2025 shortfall from `-8.4354pp` to
  `-0.8031pp`, but the frozen gate does not treat a near miss as a pass
- 30bps and 50bps each passed their confirmation 1/2 and full-history 3/5
  annual gates. Confirmation compounded strategy/Nasdaq returns were
  `+62.7505%`/`+32.1553%` at 10bps, `+53.0155%`/`+32.1553%` at 30bps, and
  `+43.8613%`/`+32.1553%` at 50bps; all confirmation compounded gates passed
- full-history compounded strategy/Nasdaq returns were
  `+294.6549%`/`+63.1210%` at 10bps, `+240.5100%`/`+63.1210%` at 30bps, and
  `+193.7814%`/`+63.1210%` at 50bps; all full-history compounded gates passed
- 10bps maximum drawdown was `-30.3312%` versus Nasdaq `-35.4928%`; both
  drawdown gates passed
- APP remained the largest contributor at `+33.2198pp`. Removing it and
  leaving its weight in cash produced `+197.1555%` strategy return versus
  `+63.1210%` Nasdaq, so leave-one-out excess was `+134.0345pp` and passed
- because the 10bps annual gates failed,
  `historical_gate_status=BLOCKED`, `release_status=BLOCKED`, and
  `promotion_eligible=false`. Never rerun v16, weaken the gates, or describe
  the contaminated confirmation interval as untouched.

### Post-v16 cost diagnosis, v17 negative result, and frozen v18 architecture

The read-only v16 cost diagnostic did not replay the strategy. It inverted the
frozen nav/cost equation from the emitted daily return, turnover, and cost rate;
the reconstructed gross path reconciled independently from 10/30/50bps at
absolute tolerance `1e-12`.

- diagnostic manifest:
  `output/research_only/v16/post_confirmation_diagnostic_20260829/manifest.json`,
  SHA256
  `30d2c62153ef6a463f0d3bf257077636c3f65a1662629d6c3a2f759fa3b9ccd0`
- v16's 2025 pre-cost return was `+21.7507%` versus Nasdaq `+20.3575%`, but
  10bps execution reduced it to `+19.5544%`; total return drag was `2.1962pp`
- the QQQ cash layer improved net v14 return by `+7.6323pp`, added only
  `1.9980` turnover and `0.3679pp` incremental cost drag. The dominant cost was
  nine stock-to-stock monthly rebalances, whose cost-free counterfactual added
  about `1.7102pp`
- on v14's 42 cash sessions, v16 used QQQ for 20 sessions and improved the
  strategy subset return from `-2.0193%` to `+3.9460%`, while Nasdaq returned
  `+10.4887%`. This identifies the near-miss mechanism but cannot change v16

v17 tested whether capping only stock-to-stock turnover could retain the same
signals while reducing cost. Initial allocation and every cash/QQQ regime
transition remained exact. The candidate caps were fixed at 0.50/1.00/1.50 and
only 2022-2024 was computed.

- v17 development manifest:
  `output/research_only/v17/stock_turnover_cap_development/manifest.json`,
  SHA256
  `9413031594ac17170102781dd85180856d65cf83a444a9163b8fbdd33631a818`
- 0.50 and 1.00 reduced turnover by `39.42%` and `14.87%`, but degraded 2023
  excess to `-26.5146pp` and `-20.3973pp`; 1.50 reduced turnover only `1.67%`
  and still missed both performance and reduction gates
- `selected_turnover_cap=null`,
  `all_development_gates_passed=false`, and
  `post_development_period_computed=false`. Never expand or retune v17

The next mechanism is not derived from the v16/v17 result. It source-locks the
pre-existing v7 research architecture—80% QQQ core and 20% stock satellite—
without searching a new weight grid, then applies it to current frozen v14
stock targets and current SHA-bound QQQ history.

- v18 development manifest:
  `output/research_only/v18/source_locked_v7_core_development/manifest.json`,
  SHA256
  `6a324a5923c7c3eaf20f7e0267e9ecabde476726ee4aedfef2e636ee7ba9a51b`
- 10/30/50bps all beat Nasdaq in 3/3 development years. At 10bps, excess was
  `+3.0636pp` in 2022, `+6.8885pp` in 2023, and `+18.0840pp` in 2024; maximum
  drawdown was `-31.3780%` versus Nasdaq `-35.4928%`
- versus QQQ total return, every cost won 2/3 development years and had positive
  compounded excess: `+23.1945pp`, `+20.8906pp`, and `+18.6211pp`
- the frozen protocol is
  `output/research_only/v18/frozen_protocol_20260829.json`, SHA256
  `8f6797a5d050eedd8fe64aaac19c04d6441379762d0f650fbc238f70103dec1c`
- the historical robustness interval is explicitly
  `REPEATEDLY_HUMAN_EXPOSED`, `statistically_untouched=false`, and
  `may_be_called_confirmation=false`
- gates require every cost to beat Nasdaq in both post-development years and
  all 5/5 years, beat QQQ in at least 3/5 years, produce positive compounded
  excess versus both benchmarks, pass the same drawdown limits, and retain
  positive compounded excess versus both benchmarks after removing the largest
  non-QQQ satellite contributor
- the protocol records `historical_robustness_replay_executed=false` and
  `post_development_results_inspected=false`. The exact frozen code was
  committed and pushed as `009c58b67c71b337e6d216babc351c7f5b1652c5`;
  local, `origin/master`, and remote `master` matched before execution.

### Frozen v18 one-shot contaminated robustness result

The v18 robustness replay executed exactly once after the Git convergence
above. It regenerated and matched the protocol and reconciled every 2022-2024
daily output at 10/30/50bps to absolute tolerance `1e-12` before reading later
years.

- result manifest:
  `output/research_only/v18/frozen_robustness_20260829/manifest.json`, SHA256
  `56555be15500cb7044c9c02c125df8130ff90c562e3dd9f3dc1f014bf55a526b`
- the architecture beat Nasdaq in 2022, 2023, 2024, and partial-2026 at every
  cost, but failed 2025. Annual excess was `-0.8752pp` at 10bps,
  `-1.6731pp` at 30bps, and `-2.4662pp` at 50bps. Thus every cost achieved 4/5
  versus the frozen 5/5 requirement and only 1/2 post-development years versus
  required 2/2
- full-history compounded strategy/Nasdaq/QQQ returns were
  `+116.2196%`/`+63.1210%`/`+79.6351%` at 10bps,
  `+110.5824%`/`+63.1210%`/`+79.6351%` at 30bps, and
  `+105.0898%`/`+63.1210%`/`+79.6351%` at 50bps. All full-history compounded
  gates passed
- all costs achieved the required 3/5 annual wins versus QQQ. The 10bps and
  30bps post-development compounded returns beat both benchmarks; at 50bps,
  strategy returned `+36.9751%` versus QQQ `+37.0145%`, missing that gate by
  about `0.0394pp`
- maximum drawdown was `-31.3780%` versus Nasdaq `-35.4928%`; both drawdown
  gates passed
- APP was the largest non-QQQ satellite contributor at `+7.0475pp`. Removing
  APP and leaving its weight in cash produced `+102.2393%` versus Nasdaq
  `+63.1210%` and QQQ `+79.6351%`; both satellite-removal gates passed
- because the strict 2025 annual gates and one 50bps post-development QQQ
  compound gate failed, `historical_gate_status=BLOCKED`,
  `release_status=BLOCKED`, and `promotion_eligible=false`. Never rerun v18,
  weaken its gates, or call this repeatedly exposed interval confirmation.

### Retrospective v19 source-locked v10 feasibility result

The user explicitly resumed research with a 5/5 annual-win objective. v19
does not alter or overwrite v14-v18. It binds the pre-existing v10 contrarian
alpha-budget script and all nine already-emitted neighbor-grid points, then
applies that complete grid to exact 10/30/50bps replays of the frozen v14
target schedule. The exact stock-sleeve paths reconcile every frozen v14
annual cost result with maximum absolute error
`5.329070518200751e-15`.

- implementation:
  `scripts/research_v19_source_locked_v10_feasibility.py`
- tests: `tests/test_research_v19_source_locked_v10_feasibility.py`
- source v10 script SHA256:
  `7f1ece283cc8800a1614bc4c0384d25d6699151b11ca86600a464b323a3ea2ba`
- source v10 summary SHA256:
  `82242ada4cc4ac01d9c50b4a15dc0c997cafc0ce9159bc9d24e79ba55bdbea33`
- result manifest:
  `output/research_only/v19/source_locked_v10_feasibility/manifest.json`,
  SHA256
  `6e06cd6f1fffdb6579b0716adccaf7826e704a527e92dacb6d3d8e70a8fa8e01`
- only `lookback_84_crowded_stock_0.10` passed 5/5 at every cost. The
  source-locked rule uses a normal 40% stock sleeve and reduces it to 10% when
  the preceding 84-session stock-versus-QQQ relative return is positive; all
  monthly decisions use observations strictly before the rebalance session
- annual excess versus Nasdaq at 10bps was `+3.1122pp`, `+10.3749pp`,
  `+12.3597pp`, `+3.5463pp`, and `+11.5176pp` for 2022 through partial-2026
- at 30bps the five annual excesses were `+2.7987pp`, `+8.6010pp`,
  `+11.3308pp`, `+2.4226pp`, and `+10.6500pp`
- at 50bps they were `+2.4860pp`, `+6.8454pp`, `+10.3121pp`, `+1.3092pp`,
  and `+9.7881pp`; this is 5/5 with the smallest margin in 2025
- compounded strategy/Nasdaq/QQQ returns were
  `+128.2249%`/`+63.1210%`/`+79.6351%` at 10bps,
  `+119.3418%`/`+63.1210%`/`+79.6351%` at 30bps, and
  `+110.8028%`/`+63.1210%`/`+79.6351%` at 50bps
- 10bps maximum drawdown was `-31.4724%`, versus Nasdaq's frozen
  `-35.4928%`
- `historical_fit_status=PASS`, but all nine grid points were evaluated on
  repeatedly human-exposed 2022-2026 data and only one passed. Therefore this
  is `RETROSPECTIVE_FEASIBILITY_ONLY`, `statistically_untouched=false`,
  `release_status=BLOCKED`, and `promotion_eligible=false`. The 5/5 result is
  not confirmation and does not replace any adverse v14-v18 evidence

### Temporally isolated v20 retraining and recent holdout

After the user correctly challenged the earlier claim that exposed recent data
could not be controlled in a new training run, v20 separated model-level data
access from prior human exposure. It preserves v14-v19 and retrains the
source-locked v10 grid with a hard chronological boundary:

- the frozen v14 base selector still uses a 36-month rolling plus expanding
  history from `2019-01-01`, with its first out-of-sample targets in 2022
- the overlay selector receives only 2022-2023; eligibility requires positive
  annual excess versus Nasdaq in both years at 10/30/50bps
- the deterministic ranking maximizes worst annual excess, then 50bps
  compounded excess, minimizes 50bps turnover, and finally uses the variant key
- only the selected variant is then evaluated in 2024; 2025-2026 cannot be read
  until the frozen protocol regenerates exactly and all bound inputs match
- implementation:
  `scripts/research_v20_temporal_retraining.py` and
  `scripts/research_v20_recent_holdout.py`
- tests: `tests/test_research_v20_temporal_retraining.py`
- frozen protocol:
  `output/research_only/v20/temporally_isolated_retraining_20260830/frozen_protocol.json`,
  SHA256 `1301bdd763d5ef34923fe067a8484c86cc6bf0676ac7bd6e7ca1e139b8992cc1`

The isolated selector chose `lookback_84_crowded_stock_0.20`, not v19's
retrospectively selected 10% sleeve. Its worst 2022-2023 annual excess across
all costs was `+3.5705pp`. The frozen winner then passed 2024 validation with
Nasdaq excess of `+21.6975pp`, `+20.2957pp`, and `+18.9111pp` at 10/30/50bps.
The protocol records `model_data_isolation=PASS`, while separately retaining
`researcher_exposure_status=HISTORICALLY_EXPOSED` and
`statistically_untouched=false`.

The recent holdout executed once only after that freeze:

- result manifest:
  `output/research_only/v20/recent_holdout_20260830/manifest.json`, SHA256
  `b2f19203709d3dbc7b088e1a72604a048ee83569251d49cb7f44f947a743fe3e`
- 2025 excess versus Nasdaq was `+1.5342pp` at 10bps, `+0.2557pp` at 30bps,
  and `-1.0092pp` at 50bps
- partial-2026 excess was `+11.2575pp`, `+10.2939pp`, and `+9.3374pp`
- compounded 2025 through partial-2026 excess remained positive at
  `+15.4065pp`, `+12.6965pp`, and `+10.0360pp`
- the predeclared gate required both annual excesses to be positive at every
  cost. The 2025 50bps miss therefore leaves
  `recent_holdout_status=BLOCKED`, `short_forward_phase_eligible=false`,
  `release_status=BLOCKED`, and `promotion_eligible=false`

The 10/30/50bps paths are total-execution-cost sensitivity stresses applied to
gross turnover; they are not three IBKR commission plans. A later genuine
forward phase must separately freeze the user's actual IBKR pricing plan and
record commission, exchange/regulatory fees, and fill-versus-arrival-price
slippage for each order. Keep the three historical stresses as robustness
evidence, but use the realized all-in IBKR basis points as the primary forward
cost series. IBKR paper fills are operational evidence only and cannot prove
live slippage because they are simulated without deep-book execution. This
does not authorize an IBKR connection, account access, or an order.

### Offline v21 IBKR base-commission calibration

v21 converts the frozen v20 reference portfolio into whole-share, initial
allocation plans across five account sizes and both currently published IBKR
US-stock pricing structures. It is a pure local calculation and does not use a
broker API, account, credential, paper order, or live order.

- implementation:
  `src/research/ibkr_cost_calibration.py` and
  `scripts/research_v21_ibkr_cost_calibration.py`
- tests: `tests/test_ibkr_cost_calibration.py`
- result manifest:
  `output/research_only/v21/ibkr_cost_calibration_20260830/manifest.json`,
  SHA256 `527064217750d3663b1360e5adc2c7f645e870a7400be49785b71815cceb4a14`
- the reference is the frozen 2026-07-01 target observed through 2026-07-17:
  40% stock sleeve and 60% QQQ, with no post-cutoff signal or price
- the commission schedule was read from IBKR's official US-stock commission
  page on 2026-08-30. Fixed uses `$0.005/share`, `$1/order` minimum; Tiered uses
  the lowest-volume `$0.0035/share`, `$0.35/order` minimum. The calculation
  excludes exchange/regulatory fees, spread, slippage, impact, and FX

Whole-share initial-allocation base-commission results:

| Account equity | Fixed | Tiered | Achieved exposure | Zero-share targets |
|---:|---:|---:|---:|---:|
| $10,000 | 5.0665bps | 1.8837bps | 92.9022% | 1 |
| $25,000 | 2.4695bps | 0.8643bps | 97.1860% | 0 |
| $50,000 | 1.2128bps | 0.4393bps | 98.9483% | 0 |
| $100,000 | 0.6263bps | 0.2952bps | 99.2370% | 0 |
| $250,000 | 0.3829bps | 0.2262bps | 99.7148% | 0 |

Thus the published base commission is far below the 30/50bps historical
stress assumptions at every modeled size. At $10,000, whole-share rounding and
the missing small target are more material than commission. Do not call these
numbers realized all-in costs: a future immutable ledger must add external
fees and fill-versus-arrival-midpoint slippage. Preserve
`release_status=BLOCKED`, `promotion_eligible=false`, and all no-broker/no-order
flags.

### Regularized v22 walk-forward development and future-only freeze

v22 is a new research generation created after the v20 2025 result was
inspected. It explicitly reclassifies 2025 as development data and does not
claim a new untouched historical holdout. It preserves the existing nine-point
v10 grid and adds no parameter value. Selection first requires positive annual
excess versus Nasdaq at 10/30/50bps, then admits configurations within 1.5
percentage points of the best worst-year excess and regularizes toward a
smaller crowded-regime stock sleeve followed by a longer lookback.

- implementation: `scripts/research_v22_regularized_walkforward.py`
- tests: `tests/test_research_v22_regularized_walkforward.py`
- frozen protocol:
  `output/research_only/v22/regularized_walkforward_20260830/frozen_protocol.json`,
  SHA256 `cae071fa33fdb3ebfe6255662acc677faec89ebf565f57a534f74cad8067e8bc`
- the v14 base selector still begins training at `2019-01-01`; the v22 overlay
  uses 2022-2025 and intentionally does not read or evaluate existing 2026 data
- final selection: `lookback_84_crowded_stock_0.10`, with the normal stock
  sleeve unchanged at 40% and the crowded-regime sleeve reduced to 10%

The expanding next-year diagnostic selected only from preceding years and
passed every cost cell:

| Selection years | Test year | Selected | Excess at 10/30/50bps |
|---|---:|---|---:|
| 2022 | 2023 | 84 / 20% | +7.5748 / +5.8053 / +4.0541pp |
| 2022-2023 | 2024 | 84 / 10% | +12.3597 / +11.3308 / +10.3121pp |
| 2022-2024 | 2025 | 84 / 10% | +3.5463 / +2.4226 / +1.3092pp |

The final 84/10% configuration is positive versus Nasdaq in every 2022-2025
year at every cost. At 50bps the annual excesses are `+2.4860pp`, `+6.8454pp`,
`+10.3121pp`, and `+1.3092pp`; compounded strategy return is `+76.2707%`
versus Nasdaq `+48.5589%`, a `+27.7118pp` difference. Thus
`walk_forward_status=PASS`, `development_status=PASS`, and
`research_forward_observation_ready=true`.

This is improved robustness evidence, not clean confirmation: the conservative
selection rule was designed after the 2025 result was known, so
`historical_selection_contaminated=true`, `statistically_untouched=false`,
`release_status=BLOCKED`, and `promotion_eligible=false` remain mandatory.
The frozen future protocol accepts only data after `2026-08-30`, uses 30bps as
the primary total-cost envelope and 50bps as stress, and targets 3 operational
months plus a 6-month decision point. The v21 worst modeled base commission is
5.0665bps, leaving 24.9335bps inside the 30bps envelope for fees, spread,
slippage, and impact; this is not a realized-cost measurement.

Do not change v20's selection rule, replace the isolated 20% winner with the
known v19 10% winner, weaken the 50bps gate, or rerun the recent holdout. The
v20 result proves that chronological retraining is feasible and repairs
model-level leakage, but it also shows why prior human exposure must remain a
separate disclosure.

If promotion later passes, start small with position/exposure caps, no leverage
by default, manual review, and automatic stop conditions. This does not
authorize broker access now.

### v23-v48 stock-only optimization and prospective freeze

The later continuation moved from v22's mixed architecture to a stock-only
Top-5 selector. Every numbered generation retained an immutable protocol or
negative result, which is why the audit sequence reached the forties without
forty independent strategy improvements.

The final pre-v48 experiments were:

- v44/v45 monthly rank buffers: rejected. A 2x buffer reduced turnover from
  66.3038 to 56.9352 but passed only 4/6 training years; a 3x buffer passed
  only 3/6. Reduced churn did not offset stale-signal alpha loss.
- v46 monthly-entry loss stop: the fixed 20% candidate beat Nasdaq in all six
  2020-2025 training years at 50bps, but maximum drawdown was 34.1158%, missing
  the frozen 33.6408% ceiling by 0.4751 percentage points.
- v47 hybrid: combined that already-selected 20% entry stop with the
  already-tested 25% portfolio stop. It searched no new threshold and passed
  all annual, compounded-return, drawdown, and expanding-year gates.

At the 50bps stress cost, v47's annual training excess versus Nasdaq was:

| Year | Excess vs Nasdaq |
|---:|---:|
| 2020 | +5.3068pp |
| 2021 | +9.8594pp |
| 2022 | +20.6767pp |
| 2023 | +0.0109pp |
| 2024 | +7.3515pp |
| 2025 | +4.7116pp |

The 2023 margin is very thin and must not be described as strong robustness.
Across 2020-2025, the 50bps compounded strategy return was +317.6699% versus
Nasdaq +159.0330%; maximum drawdown was 32.8123%. These are training results,
not official forward wins.

Immutable artifacts:

- v47 protocol:
  `output/research_only/v47/hybrid_entry_portfolio_stop_20260830/frozen_protocol.json`,
  SHA256 `bac6799132340cbb855f6aa4b1f829dde05e586507202d90d4be65a189388190`
- v47 result manifest:
  `output/research_only/v47/hybrid_entry_portfolio_stop_20260830/development_results/manifest.json`,
  SHA256 `58f67e1c0294557925c6c0b21849d39d145612039b3bc0bbe80c681165c0a336`
- v48 runner: `scripts/research_v48_isolated_prospective_v47_observation.py`
- v48 protocol:
  `output/research_only/v48/v47_prospective_20260830/frozen_protocol.json`,
  SHA256 `e717ff441b9b3804579df35fc470d5f961d8b09402691a3704de1dca649978ef`
- v48 append-only ledger:
  `output/research_only/v48/v47_prospective_20260830/prospective_ledger.jsonl`
- v43 supersession record:
  `output/research_only/v43/v28_prospective_20260830/superseded_by_v48.json`,
  SHA256 `fbe49cbd1fca71a16b86e2ca6039413e743ec3a522b1459dab5563a438bc6c30`

The v48 runtime reuses v43's isolated Company Facts cache, exact QQQ cutoff,
strict signal/execution chronology, deterministic signal recovery, and
append-only hash chain. Its valuation adapter is bound to v47's fixed hybrid
risk replay. The protocol and runner continue to forbid ETF holdings, broker
actions, and promotion while observation is incomplete.

### v49 January-July 2026 exposed out-of-sample result

The exact frozen v47 hybrid model had not previously been replayed on
January-July 2026. Its parameters were selected exclusively on 2020-2025, so
v49 correctly uses those seven months as parameter-out-of-sample evidence.
Earlier 2026 diagnostics had motivated the stop architecture, so the evidence
class is `RESEARCHER_EXPOSED_OUT_OF_SAMPLE`, not pristine forward evidence.

Before computing the result, v49 froze these gates: positive compounded excess
versus Nasdaq at 30/50bps, at least 4/7 monthly wins at 50bps, no more than 10
percentage points of maximum-drawdown lag versus Nasdaq, absolute drawdown no
worse than 33.6408%, complete monthly coverage, and no ETF targets. Passing
every gate would have reduced the first decision checkpoint to one complete
true-prospective month; any failure preserved the three-month checkpoint.

The unique replay was run once and retained without parameter changes. At
50bps:

- strategy return: +28.9309%
- Nasdaq return: +9.1725%
- compounded excess: +19.7584 percentage points
- monthly wins: 4/7
- strategy maximum drawdown: -30.5951%
- Nasdaq maximum drawdown: -12.8380%
- drawdown lag: 17.7572 percentage points
- stock stop exits: 7; portfolio stop exits: 0

All gates passed except the 10-point relative-drawdown gate. Therefore
`observation_status=BLOCKED` and
`decision_consequence=THREE_COMPLETE_TRUE_PROSPECTIVE_MONTHS_REMAIN_REQUIRED`.
This does not erase the positive seven-month return evidence, but it does mean
that the available history does not justify shortening the true prospective
checkpoint to one month.

Immutable v49 artifacts:

- protocol:
  `output/research_only/v49/v47_2026_exposed_oos_20260830/frozen_protocol.json`,
  SHA256 `2f248591e90774ad279fbd1450503e8a86a25ca6a937d72ddafa0f68503b3ec0`
- result manifest:
  `output/research_only/v49/v47_2026_exposed_oos_20260830/observation_results/manifest.json`,
  SHA256 `2d8d9c8c0200981745da405bd0f25530aba352d7cbd90866e87b1edb07c30655`
- implementation: `scripts/research_v49_v47_2026_exposed_oos.py`
- tests: `tests/test_research_v49_v47_2026_exposed_oos.py`

### v50r1 corrected v47 implementation and successor protocol

The external review identified five defects that were material even though
they did not change the frozen v30 target schedule in the available training
sample:

1. integer-ratio split inference could reinterpret a real crash as a corporate
   action;
2. the historical $10 eligibility gate could use future-adjusted price units;
3. a monthly rebalance could run before an already-triggered stop and re-enter
   the same position at that close;
4. a late SIGNAL bundle could stage a universe not actually available on the
   declared signal date; and
5. frozen bindings used checkout-local absolute paths.

v50r1 is a repair of the already selected v47 candidate, not a new parameter
search. It keeps v30's fixed monthly Top-5 selector and v47's 20% entry-loss /
25% portfolio-stop thresholds. `src/research/corrected_stock_policy.py` now:

- builds continuous return prices only from source-adjudicated `CONFIRMED`
  company actions while preserving `CONFIRMED_MARKET_MOVE` crashes;
- applies minimum-price eligibility in contemporaneous nominal units;
- fails closed when an unresolved event affects the ranked top-25 pool or a
  replay target;
- checks pending stops before a coincident monthly rebalance and vetoes
  same-close re-entry;
- rejects missing monthly entry closes and defers suspended-name exits instead
  of manufacturing a stale-price fill.

`scripts/research_v50_corrected_v47.py` additionally requires every SIGNAL
bundle to be created on the same UTC date as its declared signal, stores
repository-relative source bindings, and maintains the same append-only hash
chain used by v48. The old v48 ledger contained only `PROTOCOL_FROZEN` and zero
signals, so it was explicitly superseded before any prospective observation
was contaminated.

The corrected development replay retained all 348 frozen monthly target rows
with zero target differences. At the 50bps stress cost, 2020-2025 annual
excesses versus Nasdaq are `+5.3068pp`, `+9.8594pp`, `+20.6767pp`,
`+0.0109pp`, `+7.3515pp`, and `+4.7116pp`. Compounded strategy return is
`+317.6699%` versus Nasdaq `+159.0330%`; maximum drawdown is `32.8123%`, below
the frozen `33.6408%` ceiling. The very small 2023 margin remains a robustness
warning. All of this is corrected training evidence only: official prospective
wins remain zero, `release_status=BLOCKED`, and `promotion_eligible=false`.

Immutable v50r1 artifacts:

- implementation commit: `69de9b1f637f6acce982d63bf164ba4968c623ca`
- development protocol:
  `output/research_only/v50/corrected_v47_20260831_r1/development_protocol.json`,
  SHA256 `474d8a9646c5fce589c7bd8dcfd83c872238e2904b6eadcb79a96ef47465e323`
- development manifest:
  `output/research_only/v50/corrected_v47_20260831_r1/development_results/manifest.json`,
  SHA256 `17ecbf2c2c772b6e56a4464e3c3e0626a0e4695fba999a3b7fd4d44934ec9c9e`
- prospective protocol:
  `output/research_only/v50/corrected_v47_20260831_r1/frozen_protocol.json`,
  SHA256 `b9ea71eb5f6b3cd1104648e50c321356bf6d8c198f58673bd71821dc47b9502a`
- append-only prospective ledger:
  `output/research_only/v50/corrected_v47_20260831_r1/prospective_ledger.jsonl`
- v48 supersession record:
  `output/research_only/v48/v47_prospective_20260830/superseded_by_v50.json`,
  SHA256 `6a7b5570660acddc84e86d932062560eb6d4aba7fd534305dc799604e6873ef1`

The current v50r1 status is `WAITING_FOR_FIRST_PROSPECTIVE_SIGNAL`. The first
permitted signal date is `2026-08-31`; its bundle must be staged after that
Nasdaq close while the UTC date is still `2026-08-31`. Missing that window must
fail closed rather than be backfilled. In Beijing/Singapore time this is
`2026-09-01 04:00-08:00`, not the early morning of `2026-08-31`. Targeted
v24/v42/v47/v48/v50 regression
coverage passes. The full suite has only the two pre-existing external-archive
failures that require the absent local file
`/Users/bytedance/Downloads/stooq_d_us_txt_2026-08-08.zip`.

### 2026-09-04 missed-window recovery and scheduling repair

The `2026-08-31` original prospective window was missed. The old Codex cron
displayed an Asia/Singapore schedule but its executions landed around
`04:30-07:30 UTC` (`12:30-15:30` Singapore), after the required same-UTC-date
window. No August SIGNAL was written, and the v50r1 ledger remains at zero
signals, zero executions, and zero valuations. Its SHA256 remains
`55336d125e68ddca4ba89119055d1ad5d7beac9dd86e52d81e2dcb61cf426b53`.

The `quant-stocks` automation is now ACTIVE with an explicit UTC schedule. It
starts on `2026-09-28` and checks at `21:30`, `22:30`, and `23:30 UTC` on US
weekdays. It explicitly forbids an August backfill. The first remaining legal
v50r1 SIGNAL date is `2026-09-30`; if that same-UTC-date window is missed, it
must fail closed again. There are no scheduled runs before September 28.

A separate v51r9 late diagnostic was frozen before inspecting its target and
completed successfully. It reconstructs the frozen v50r1 selection using only
information available through `2026-08-31`, but it is explicitly
`LATE_RECONSTRUCTION_NOT_ORIGINAL_PROSPECTIVE_SIGNAL`. It cannot enter the v50
ledger, cannot count as an August prospective win, and cannot authorize a
broker action or order. Its equal-weight target is:

- `DASH` 20%
- `ABNB` 20%
- `AMGN` 20%
- `PANW` 20%
- `WDAY` 20%

The reconstructed market regime is ON. The audit repaired the literal ticker
`NA` for Nano Labs from an independent historical Nasdaq snapshot; excluded
BNBX using signal-date-valid SEC evidence of its July 14 Nasdaq suspension;
excluded 68 other July-snapshot names only after confirming both that their
last price predated August 31 and that none appeared in a frozen 4,137-symbol
Nasdaq screener response captured on September 4; retained SEC-unmapped names
unless covered by that eligibility rule; and removed 161 annual plus 156
quarterly rows whose `available_date` was after August 31. No strategy
parameter changed.

Immutable/retained v51r9 artifacts:

- runner commit: `6934800c725236fe055c0cd1459bb76c34bb43fa`
- recovery protocol:
  `output/research_only/v51/august_recovery_20260904_r9/frozen_recovery_protocol.json`,
  SHA256 `5f1e8379679e68cf3e2334a81deae9fa499b6ffe4f0894f1879e3853df4c41e3`
- diagnostic report:
  `output/research_only/v51/august_recovery_20260904_r9/august_2026_late_diagnostic.json`,
  SHA256 `5df3e9e6547015b09c28b4563457d122df0e44b029cf3259e829f231a982e841`
- bundle manifest SHA256:
  `1eff037d649e977bd509ca46f1b1feb0c8cd7c414285d68061e2e45cb3fb79f6`
- current Nasdaq evidence:
  `output/research_only/v51/source_evidence/nasdaq_screener_2026-09-04.json`,
  SHA256 `2f46d17855155c5ff28111393ae11725e33e8b8a55108c6da8eefddce21e9d90`

After successful validation, redundant failed staging was moved to
`/Users/bytedance/.Trash/quant-stocks-v51-failed-staging-20260904` (about
4.7GB, recoverable). The retained v51 output is about 915MB and still passes
the r9 protocol/status verification.

The first true-prospective holding month can now only begin after the valid
September 30 SIGNAL. October is the earliest complete operational-sanity
month; October-December are the earliest possible three complete prospective
months. The late August diagnostic does not shorten that checkpoint.

### 2026-09-05 v50r2 runtime repair and r1 supersession

Re-running the v51 recovery path exposed a defect that would also have
stopped the official September signal: `scripts/research_v42_prospective_v28_observation.py`
stores `available.dropna().le(stamp).all()` in the SIGNAL bundle manifest
(`readiness_gates.fundamentals_no_future_available_date`). That value is a
NumPy boolean, so the manifest `json.dumps` raises `TypeError` after every
download has completed, and the staging `except` branch discards the staged
data. v51's r8 attempt failed exactly this way; v51 fixed it only inside its
own script.

v50r2 repairs this without modifying any SHA-bound r1 file:

- new runner: `scripts/research_v50r2_corrected_v47.py`; the r1 runner,
  its development protocol, and its development replay are reused verbatim
  and remain independently verifiable (`research_v50_corrected_v47.py status`
  still passes)
- inside the isolated v43 runtime, `json.dumps` normalizes NumPy scalars to
  Python scalars; serializable payloads are emitted byte-for-byte unchanged,
  so ledger hashes are unaffected
- the missed `2026-08-31` window is recorded as `MISSED_WINDOW_NOT_BACKFILLED`;
  `stage-bundle` refuses any SIGNAL dated before `2026-09-30`, and the bundle
  validator refuses SIGNAL bundles for missed or pre-r2 dates
- `FIRST_PROSPECTIVE_SIGNAL_DATE=2026-09-30`; same-UTC-date staging still
  applies (Beijing/Singapore `2026-10-01 04:00-08:00`; start no earlier than
  `04:30` to avoid freezing a provisional close)
- no selector, threshold, cost, or development-window change;
  `release_status=BLOCKED`, `promotion_eligible=false`

Immutable v50r2 artifacts:

- implementation commit: `2cd06c698fd8b8d8a86b7d421ddeb0949628a999`
- prospective protocol:
  `output/research_only/v50/corrected_v47_20260905_r2/frozen_protocol.json`,
  SHA256 `48fef8da7ae8a7e221bdac4dff7b551c2485961ade11afa72d86539f0a69a4e7`
- append-only prospective ledger:
  `output/research_only/v50/corrected_v47_20260905_r2/prospective_ledger.jsonl`
  (only `PROTOCOL_FROZEN`)
- v50r1 supersession record:
  `output/research_only/v50/corrected_v47_20260831_r1/superseded_by_v50r2.json`,
  SHA256 `898fd80b608ce10c06f6fa0d79432613743bb4acd0522da0e3561fde44624613`
- tests: `tests/test_research_v50r2_corrected_v47.py` (the closure test
  verifies every binding is repository-relative, git-tracked, and hash-bound)

Known residual items not addressed by r2:

- the UTC scheduler that is now `ACTIVE` still lives outside the repository.
  `scripts/research_v50r2_scheduled_run.py` removes the time-zone dependency:
  invoked hourly by any scheduler, `check` decides from the UTC clock, the
  Nasdaq calendar (official closes, early closes included), and the ledger
  whether a SIGNAL or MARK is due and exits `2` when a SIGNAL window was
  missed; `run` executes the due step. Point the external scheduler at `run`
  and route a non-zero `check` exit to an alert. Tests in
  `tests/test_research_v50r2_scheduled_run.py` cover EDT and EST windows,
  early closes, the missed-window path, and never-backfill
- v51's protocol, diagnostic report, and screener evidence are not tracked, so
  8 of 13 v51 tests fail on a clean checkout
- `src/research/universe_history.py:46,94` read historical snapshots with a
  plain `read_csv`, so a literal `NA` ticker becomes a phantom `NAN` symbol in
  training-path snapshots. The live SIGNAL path is unaffected
  (`research_v42:733` uses `keep_default_na=False`). The file is in the
  `can-slim-top3-v1` strategy fingerprint closure, so fix it only as a
  recorded change

## 10. Exact next-task workflow

1. Verify the formal SHAs and every v14-v19 manifest/protocol/result SHA above.
   Never rerun or overwrite v14-v18.
2. Preserve v16's 2025 `-0.8031pp` and v18's 2025
   `-0.8752/-1.6731/-2.4662pp` cost-stressed failures. Do not change their
   parameters, targets, costs, or gates after inspection.
3. v17 proved that naive turnover caps destroy more signal alpha than they save
   in costs. Do not expand that grid or relabel it as an execution fix.
4. The v18 result may not be called confirmation. Since 2025-2026 is repeatedly
   exposed, another historical hypothesis cannot supply clean validation.
5. Preserve v19's retrospective 5/5 result and its isolated one-of-nine grid
   selection together. Never relabel `historical_fit_status=PASS` as release or
   promotion evidence.
6. Preserve v20's isolated 20% selection, its 2024 validation pass, and its
   narrow 2025 50bps recent-holdout failure. Do not retune or rerun after
   inspecting that result.
7. Preserve v47's frozen stock selector and 20% entry/25% portfolio hybrid
   stop. Do not add a new threshold or retune after the v48 protocol freeze;
   do not relabel the 6/6 training result as clean confirmation.
8. v48 and v50r1 were superseded with zero signals; the operating entry is
   now `scripts/research_v50r2_corrected_v47.py`. The `2026-08-31` window was
   missed and must never be backfilled; the first remaining legal SIGNAL date
   is `2026-09-30`. Every SIGNAL bundle must be created on the same UTC date as
   its declared signal.
   Keep release and promotion blocked; broker access, trading, formal-data
   replacement, leverage, and capital allocation remain outside this workflow.
9. Use v21 only as a base-commission and whole-share feasibility reference.
   Before any execution-cost claim, replace its account-size sensitivity with
   the user's actual equity and pricing plan and use an immutable
   fill-versus-arrival-price ledger. Do not treat 10/30/50bps as broker tariffs
   or connect to IBKR without explicit authorization.
10. Accumulate one complete v50r2 prospective month for the operational sanity
    check and use three complete months as the first decision checkpoint.
    October 2026 is the earliest complete month and October-December are the
    earliest three-month checkpoint. v49
    already tested and rejected the one-month shortcut because its 17.7572pp
    relative-drawdown lag exceeded the frozen 10pp gate. Elapsed time alone
    does not pass the gate, and no historical date may be backfilled into the
    forward ledger.
11. The historical v14 freeze generator now has an explicit
   `allow_historical_code_drift=True` mode only for non-executing structural
   tests. Default generation still rejects the changed `can_slim.py` SHA, and
   execution code must never enable the bypass.

The machine previously reported 14 logical CPUs/48GiB RAM. Research outputs use
about 22GB and free disk was about 49GiB; recheck resources. The one-shot
candidate integration and large backtest writes must remain serial.

## 11. Integrity and tests

Before this continuation the repository collected 1,373 tests. The 16 new
packages add 87 tests, the GLPG extension adds six net tests, and the HCM
negative-evidence extension adds five net tests, bringing the current
collection to 1,471. The RLMD recovery adds six more tests and the SMPL
negative-evidence package adds seven. The deterministic ADUS audit-binding
upgrade adds one net test, and the GGAL negative-evidence extension adds four,
bringing the collection to 1,489. The ARGX source-lock and negative-evidence
extension adds five more, bringing the collection to 1,494. The BCTX
pre-signal extension adds three net tests, bringing the current collection to
1,497. The DKNG direct-TTM extension adds two net tests, bringing the current
collection to 1,499. The FORR comparable-basis recovery adds ten net tests,
bringing the current collection to 1,509. The GCMG predecessor-loss recovery
adds ten net tests, bringing the current collection to 1,519. Two
environment-dependent evidence replays cannot run because this external archive
is absent:

`/Users/bytedance/Downloads/stooq_d_us_txt_2026-08-08.zip`

Affected evidence-replay tests:

- `test_gden_terminal_value_replays_fixed_cash_and_vici_stock_components`
- `test_vape_bnc_evidence_replays_sec_identity_and_stooq_tail`

All new and modified tests pass together. The full suite with exactly those two
tests deselected passed all **1,495** runnable tests after the BCTX extension.
After the DKNG extension, the corresponding runnable count is **1,497**.
After the FORR recovery, all **1,507** runnable tests passed with exactly the
same two external-archive tests deselected.
After the GCMG recovery, the corresponding runnable count is **1,517**.
All 1,517 runnable tests passed with the same two tests deselected.
The HIMX IFRS TTM recovery adds nine tests, bringing collection to **1,528**
and the corresponding runnable count to **1,526** with exactly the same two
external-archive tests deselected.
The HRMY annual-plus-M9 recovery adds ten tests, bringing collection to
**1,538** and the corresponding runnable count to **1,536** with exactly the
same two external-archive tests deselected.
The HRZN audited annual recovery adds ten tests, bringing collection to
**1,548** and the corresponding runnable count to **1,546** with exactly the
same two external-archive tests deselected.
The LAKE annual-plus-M9 recovery adds ten tests, bringing collection to
**1,558** and the corresponding runnable count to **1,556** with exactly the
same two external-archive tests deselected.
The LGO audited direct-annual recovery adds ten tests, bringing collection to
**1,568** and the corresponding runnable count to **1,566** with exactly the
same two external-archive tests deselected.
The subsequent NCTY, PLL, QFIN, RCEL, STNE, ROAD, RCON, SBET, and SOHU packages
each add ten tests. The current collection is therefore **1,658**; run all
**1,656** runnable tests with exactly the same two external-archive tests
deselected before checkpointing this continuation. All **1,656** runnable tests
passed after the SOHU recovery.
The universe negative-evidence package adds six tests, ITOS adds six, protocol
freeze adds five, and the exact-signal exclusion, deterministic tie break,
target-attribution replay, and frozen gate evaluator add one each. Current
collection is **1,679**; the corresponding runnable count is **1,677** with
exactly the same two external-archive tests deselected. All **1,677** runnable
tests passed at the protocol-freeze checkpoint.

The attribution-window regression, v15 development tests, v16 development,
freeze, and confirmation tests, plus the historical v14 default-drift guard,
add 13 tests. Current collection is **1,692**; the corresponding runnable count
is **1,690** with exactly the same two external-archive tests deselected. All
**1,690** runnable tests passed after the v16 result checkpoint.

The v16 cost diagnostic, v17 capped-execution development, v18 source-locked
development, freeze, and robustness evaluator add 14 tests. Current collection
is **1,706**; the corresponding runnable count is **1,704** with exactly the
same two external-archive tests deselected. All **1,704** runnable tests passed
after the frozen v18 robustness result.

The v19 source-locked retrospective feasibility implementation adds three
tests. Current collection is **1,709**; the corresponding runnable count is
**1,707** with exactly the same two external-archive tests deselected. All
**1,707** runnable tests passed after the v19 5/5 feasibility result.

The v20 temporally isolated retraining and recent-holdout implementation adds
five tests. Current collection is **1,714**; the corresponding runnable count
is **1,712** with exactly the same two external-archive tests deselected. All
**1,712** runnable tests passed after the frozen v20 recent-holdout result.

The v21 offline IBKR commission calibration adds six tests. Current collection
is **1,720**; the corresponding runnable count is **1,718** with exactly the
same two external-archive tests deselected. All **1,718** runnable tests passed
after the v21 offline calibration result.

The v22 regularized walk-forward implementation adds five tests. Current
collection is **1,725**; the corresponding runnable count is **1,723** with
exactly the same two external-archive tests deselected. All **1,723** runnable
tests passed after the v22 protocol freeze. `git diff --check` passed, and the
four formal financial/coverage SHAs remained identical to the invariants above.
Do not report the two absent-archive tests as passing. Record `git diff --check`
and the formal SHA recheck in the final response.

After v23-v48, current collection is **1,847**. Exactly two long-standing tests
whose required external archives are absent remain explicitly deselected, so
the runnable count is **1,845**. All **1,845** runnable tests passed after the
v48 protocol freeze. The focused v42-v48 regression was **40/40**, Python
compilation passed for the v44-v48 runners, and `git diff --check` passed. Do
not report the two deselected external-archive tests as passing.

v49 adds five tests, bringing collection to **1,852** and the runnable count to
**1,850** after the same two explicit deselections. All **1,850** runnable tests
passed after the one-shot v49 result. The focused v27/v32/v34/v41/v47-v49
regression was **29/29**, v49 compilation passed, and `git diff --check`
passed.

## 12. Git and local-artifact scope

The worktree contains many pre-existing untracked artifacts. Do not clean them
and never use broad `git add .` or `git add -A`.

Explicitly stage only this handoff, the 16 section-4 scripts, and their 16
tests. Do not stage `output/`, `tmp/`, root generated CSV/JSON files, raw market
data, or repeated candidate directories. Push `master`, then verify local HEAD,
`origin/master`, and remote `refs/heads/master` are identical.

For the 2026-08-27 GLPG checkpoint, stage only this handoff plus
`scripts/research_v14_glpg_direct_ttm_loss.py` and
`tests/test_research_v14_glpg_direct_ttm_loss.py`.

For the 2026-08-27 HCM checkpoint, stage only this handoff plus
`scripts/research_v14_hcm_direct_ttm_loss.py` and
`tests/test_research_v14_hcm_direct_ttm_loss.py`. Do not stage the source cache,
evidence outputs, or the zero-insert candidate rewrite.

For the 2026-08-27 RLMD checkpoint, stage only this handoff plus
`scripts/research_v14_rlmd_transition_ttm_loss.py` and
`tests/test_research_v14_rlmd_transition_ttm_loss.py`. Do not stage `output/`
or the raw SEC XML source cache.

For the 2026-08-27 SMPL checkpoint, stage only this handoff plus
`scripts/research_v14_smpl_acquisition_basis_gap.py` and
`tests/test_research_v14_smpl_acquisition_basis_gap.py`. For the following
ADUS checkpoint, stage only this handoff plus the already tracked
`scripts/research_v14_adus_pit_unrecoverable.py` and
`tests/test_research_v14_adus_pit_unrecoverable.py`. Do not stage either
issuer's local SEC source cache or generated manifests.

For the following GGAL checkpoint, stage only this handoff plus the already
tracked `scripts/research_v14_ggal_ias29_quarters.py` and
`tests/test_research_v14_ggal_ias29_quarters.py`. Do not stage its local SEC
source cache, generated manifest, or other `output/` artifacts.

For the following ARGX checkpoint, stage only this handoff plus the already
tracked `scripts/research_v14_argx_quarterly_reports.py` and
`tests/test_research_v14_argx_quarterly_reports.py`. Do not stage its 17 local
SEC source archives, generated manifest, or other `output/` artifacts.

For the following BCTX checkpoint, stage only this handoff plus the already
tracked `scripts/research_v14_bctx_annual_ttm_loss.py` and
`tests/test_research_v14_bctx_annual_ttm_loss.py`. Do not stage its four local
SEC source archives, generated manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the following DKNG checkpoint, stage only this handoff plus the already
tracked `scripts/research_v14_dkng_historical_cik_quarters.py` and
`tests/test_research_v14_dkng_historical_cik_quarters.py`. Do not stage the
historical Company Facts payload, S-4/A source archive, generated manifest,
candidate rewrite, audit outputs, or other `output/` artifacts.

For the following FORR checkpoint, stage only this handoff plus
`scripts/research_v14_forr_asc605_ttm_growth.py` and
`tests/test_research_v14_forr_asc605_ttm_growth.py`. Do not stage the three
SEC source archives, generated evidence manifest, candidate rewrite, audit
outputs, or other `output/` artifacts.

For the following GCMG checkpoint, stage only this handoff plus
`scripts/research_v14_gcmg_predecessor_ttm_loss.py` and
`tests/test_research_v14_gcmg_predecessor_ttm_loss.py`. Do not stage the S-1,
8-K exhibit, generated evidence manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the following HIMX checkpoint, stage only this handoff plus
`scripts/research_v14_himx_ifrs_ttm_growth.py` and
`tests/test_research_v14_himx_ifrs_ttm_growth.py`. Do not stage the 20-F, the
two 6-K exhibits, generated evidence manifest, candidate rewrite, audit
outputs, or other `output/` artifacts.

For the following HRMY checkpoint, stage only this handoff plus
`scripts/research_v14_hrmy_annual_m9_ttm_growth.py` and
`tests/test_research_v14_hrmy_annual_m9_ttm_growth.py`. Do not stage the 10-K,
the two 10-Q filings, generated evidence manifest, candidate rewrite, audit
outputs, or other `output/` artifacts.

For the following HRZN checkpoint, stage only this handoff plus
`scripts/research_v14_hrzn_exact_annual_growth.py` and
`tests/test_research_v14_hrzn_exact_annual_growth.py`. Do not stage the 10-K,
generated evidence manifest, candidate rewrite, audit outputs, or other
`output/` artifacts.

For the following LAKE checkpoint, stage only this handoff plus
`scripts/research_v14_lake_annual_m9_ttm_growth.py` and
`tests/test_research_v14_lake_annual_m9_ttm_growth.py`. Do not stage the 10-K,
two 10-Q filings, generated evidence manifest, candidate rewrite, audit
outputs, or other `output/` artifacts.

For the following LGO checkpoint, stage only this handoff plus
`scripts/research_v14_lgo_exact_annual_growth.py` and
`tests/test_research_v14_lgo_exact_annual_growth.py`. Do not stage the Form
40-F cover, Exhibit 99.69, generated evidence manifest, candidate rewrite,
audit outputs, or other `output/` artifacts.

For the following NCTY checkpoint, stage only this handoff plus
`scripts/research_v14_ncty_h1_ttm_growth.py` and
`tests/test_research_v14_ncty_h1_ttm_growth.py`. Do not stage the two 20-F
filings, three 6-K exhibits, generated evidence manifest, candidate rewrite,
audit outputs, or other `output/` artifacts.

For the following PLL checkpoint, stage only this handoff plus
`scripts/research_v14_pll_exact_ttm_loss.py` and
`tests/test_research_v14_pll_exact_ttm_loss.py`. Do not stage the 20-F, 6-K
exhibit, generated evidence manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the following QFIN checkpoint, stage only this handoff plus
`scripts/research_v14_qfin_m9_ttm_growth.py` and
`tests/test_research_v14_qfin_m9_ttm_growth.py`. Do not stage the two 6-K
exhibits, generated evidence manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the following RCEL checkpoint, stage only this handoff plus
`scripts/research_v14_rcel_exact_annual_loss.py` and
`tests/test_research_v14_rcel_exact_annual_loss.py`. Do not stage the 20-F,
20-F/A, generated evidence manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the following STNE checkpoint, stage only this handoff plus
`scripts/research_v14_stne_q3_2020.py` and
`tests/test_research_v14_stne_q3_2020.py`. Do not stage the 6-K exhibit,
generated evidence manifest, candidate rewrite, audit outputs, or other
`output/` artifacts.

For the following ROAD checkpoint, stage only this handoff plus
`scripts/research_v14_road_previous_gaap_ttm_growth.py` and
`tests/test_research_v14_road_previous_gaap_ttm_growth.py`. Do not stage the
10-Q source, Company Facts cache, generated evidence manifest, candidate
rewrite, audit outputs, or other `output/` artifacts.

For the following RCON checkpoint, stage only this handoff plus
`scripts/research_v14_rcon_h1_ttm_loss.py` and
`tests/test_research_v14_rcon_h1_ttm_loss.py`. Do not stage the 20-F, 6-K
exhibit, generated evidence manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the following SBET checkpoint, stage only this handoff plus
`scripts/research_v14_sbet_accounting_acquirer_loss.py` and
`tests/test_research_v14_sbet_accounting_acquirer_loss.py`. Do not stage the
proxy, merger-close exhibit, generated evidence manifest, candidate rewrite,
audit outputs, or other `output/` artifacts.

For the following SOHU checkpoint, stage only this handoff plus
`scripts/research_v14_sohu_restated_quarters.py` and
`tests/test_research_v14_sohu_restated_quarters.py`. Do not stage the four 6-K
exhibits, generated evidence manifest, candidate rewrite, audit outputs, or
other `output/` artifacts.

For the protocol-freeze checkpoint, stage exactly this handoff plus:

- `src/research/can_slim.py`
- `src/research/can_slim_walk_forward.py`
- `scripts/research_v14_adaptive_pretrain.py`
- `scripts/research_v14_universe_gap_negative_evidence.py`
- `scripts/research_v14_itos_zero_revenue_growth.py`
- `scripts/research_v14_freeze_protocol.py`
- `scripts/research_v14_frozen_replay.py`
- `tests/test_can_slim.py`
- `tests/test_can_slim_walk_forward.py`
- `tests/test_research_v14_universe_gap_negative_evidence.py`
- `tests/test_research_v14_itos_zero_revenue_growth.py`
- `tests/test_research_v14_freeze_protocol.py`
- `tests/test_research_v14_frozen_replay.py`

Do not stage the generated protocol JSON, MOMO/ITOS/other source caches and
manifests, the universe catalogs, candidate/audit artifacts, global walk-forward
CSVs, `output/`, `tmp/`, or any root generated CSV/JSON file. After push, run
the one-shot replay from the committed code and checkpoint its result
separately without changing the frozen protocol.

For the v16 pre-confirmation checkpoint, stage exactly this handoff plus:

- `src/research/can_slim.py`
- `tests/test_can_slim.py`
- `scripts/research_v15_benchmark_core_development.py`
- `tests/test_research_v15_benchmark_core_development.py`
- `scripts/research_v16_trend_confirmed_qqq_development.py`
- `tests/test_research_v16_trend_confirmed_qqq_development.py`
- `scripts/research_v16_freeze_protocol.py`
- `tests/test_research_v16_freeze_protocol.py`
- `scripts/research_v16_frozen_confirmation.py`
- `tests/test_research_v16_frozen_confirmation.py`

Do not stage generated v15/v16 manifests, protocols, daily results, target
schedules, any other `output/` artifact, `tmp/`, or root generated CSV/JSON
files. Push and verify all three Git refs before executing the one-shot v16
confirmation.

For the v16 result checkpoint, stage exactly this handoff plus the historical
v14 freeze-test compatibility change:

- `scripts/research_v14_freeze_protocol.py`
- `tests/test_research_v14_freeze_protocol.py`

Do not stage the v16 result manifest or any generated output. The compatibility
mode must remain explicit and test-only; default v14 generation must continue
to reject runtime-code drift.

For the v18 pre-robustness checkpoint, stage exactly this handoff plus:

- `scripts/research_v16_post_confirmation_diagnostic.py`
- `tests/test_research_v16_post_confirmation_diagnostic.py`
- `scripts/research_v17_stock_turnover_cap_development.py`
- `tests/test_research_v17_stock_turnover_cap_development.py`
- `scripts/research_v18_source_locked_v7_core_development.py`
- `tests/test_research_v18_source_locked_v7_core_development.py`
- `scripts/research_v18_freeze_protocol.py`
- `tests/test_research_v18_freeze_protocol.py`
- `scripts/research_v18_frozen_robustness.py`
- `tests/test_research_v18_frozen_robustness.py`

Do not stage generated diagnostic/development/protocol/daily/target artifacts,
any other `output/`, `tmp/`, or root generated CSV/JSON file. Verify all three
Git refs before the single contaminated v18 robustness replay.

For the v18 result checkpoint, stage only this handoff. Do not stage the result
manifest, daily outputs, targets, attribution, or any other generated artifact.

For the v19 retrospective-feasibility checkpoint, stage exactly this handoff
plus:

- `scripts/research_v19_source_locked_v10_feasibility.py`
- `tests/test_research_v19_source_locked_v10_feasibility.py`

Do not stage the v19 manifest, grid summary, decisions, daily outputs, or any
other generated `output/`, `tmp/`, or root CSV/JSON artifact.

For the v20 temporal-retraining checkpoint, stage exactly this handoff plus:

- `scripts/research_v20_temporal_retraining.py`
- `scripts/research_v20_recent_holdout.py`
- `tests/test_research_v20_temporal_retraining.py`

Do not stage the v20 frozen protocol, development/validation/holdout daily
outputs, recent-holdout manifest, any other generated `output/`, `tmp/`, or
root CSV/JSON artifact. Preserve both v20 result SHAs in this handoff.

For the v21 offline-IBKR-cost checkpoint, stage exactly this handoff plus:

- `src/research/ibkr_cost_calibration.py`
- `scripts/research_v21_ibkr_cost_calibration.py`
- `tests/test_ibkr_cost_calibration.py`

Do not stage the v21 manifest, reference portfolio, calibration CSV, any other
generated `output/`, `tmp/`, or root CSV/JSON artifact. No IBKR credential,
connection, account access, paper order, or live order belongs in this work.

For the v22 regularized-walk-forward checkpoint, stage exactly this handoff
plus:

- `scripts/research_v22_regularized_walkforward.py`
- `tests/test_research_v22_regularized_walkforward.py`

Do not stage the v22 frozen protocol, ranking, folds, selected daily paths, or
any other generated `output/`, `tmp/`, or root CSV/JSON artifact. Preserve the
v22 protocol SHA in this handoff. No broker credential, account access, paper
order, live order, or capital allocation belongs in this checkpoint.

For the v44-v48 checkpoint, stage only the four development runners and tests,
the v48 prospective runner and test, the v44-v48 frozen/result artifacts, the
v43-to-v48 supersession record, this handoff, and the two v48 staging-ignore
lines. Never stage regenerated bundles/staging work, unrelated `output/`,
`tmp/`, root CSV/JSON files, credentials, account data, or broker artifacts.

For the v49 checkpoint, stage only its implementation, tests, frozen protocol,
three observed daily paths, result manifest, and this handoff. Never rerun or
overwrite the one-shot result, and do not modify v47/v48 in response to it.
